{
  "groupId": "benchmarks-linters",
  "testedAt": "2026-03-16T17:15:00Z",
  "isolation": {
    "context": "Run CLI commands directly in the working directory. No shared state mutations."
  },
  "toolsUsed": ["powershell"],
  "assertions": [
    {
      "id": "VAL-CPU-023",
      "title": "Lint and type check pass",
      "status": "fail",
      "steps": [
        { "action": "Run uv run ruff check .", "expected": "Exit code 0", "observed": "Exit code 1, found 42 errors" },
        { "action": "Run uv run mypy src/tensor_grep", "expected": "Exit code 0", "observed": "Exit code 0, no issues found" }
      ],
      "evidence": {
        "terminalSnapshots": [
          "native-cpu-engine/benchmarks-linters/VAL-CPU-023-ruff.txt",
          "native-cpu-engine/benchmarks-linters/VAL-CPU-023-mypy.txt"
        ]
      },
      "issues": "ruff check failed with 42 errors (mostly related to formatting and unused imports in run_cli_parity_tests.py)."
    },
    {
      "id": "VAL-CPU-024",
      "title": "Benchmark regression check passes",
      "status": "pass",
      "steps": [
        { "action": "Run python benchmarks/run_benchmarks.py --output artifacts/bench_run_benchmarks.json", "expected": "Benchmarks execute successfully", "observed": "Benchmarks executed successfully" },
        { "action": "Run python benchmarks/check_regression.py --baseline auto --current artifacts/bench_run_benchmarks.json", "expected": "Exit code 0, no regression", "observed": "Exit code 0, 'No benchmark regressions detected.'" }
      ],
      "evidence": {
        "terminalSnapshots": [
          "native-cpu-engine/benchmarks-linters/VAL-CPU-024-run.txt",
          "native-cpu-engine/benchmarks-linters/VAL-CPU-024-check.txt"
        ]
      },
      "issues": null
    }
  ],
  "frictions": [],
  "blockers": [],
  "summary": "Tested 2 assertions: 1 passed, 1 failed (VAL-CPU-023: ruff linting failed on run_cli_parity_tests.py)."
}
