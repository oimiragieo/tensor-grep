# Design — #57: raise CALLER_SCAN_FILE_CEILING (512 → 2000)

**Verdict: GO, hybrid.** Sonnet verify-plan-against-code design 2026-07-09 (agent af2dcdd2), read-only, cited `file:line` vs `main` @ v1.54.3. Non-colliding (repo_map.py; no in-flight PR touches it). NOT a naive `512→2000` — 4 mandatory companion fixes + a golden-parity gate. Load-bearing (latency + ranking-fragility scar) → golden-gate before ship. Full agent output in session transcript 09e7d2aa.

## The change
`CALLER_SCAN_FILE_CEILING = 512` (`repo_map.py:168`) → **2000** (match `DEFAULT_AGENT_REPO_MAP_LIMIT`, `repo_map.py:155`). Why 2000 not higher: the map-build cost data at `repo_map.py:148` ("512=1.48s, 2000=3.51s OK, 4000=10.35s superlinear — do NOT raise past 2000 without re-measuring") sets the knee. Why not pure-deadline-driven: `--deadline` defaults to `None` on all 5 graph CLI options (`main.py:8711/8807/8871/8971/9108`), so the flag-less path has NO wall-clock bound — removing the file ceiling would reintroduce the unbounded case on the majority (no-flag) path. Hybrid = raise the default ceiling (fixes the common case) + keep it as a hard backstop for `--max-repo-files`-raised mega-repos + `--deadline` as opt-in interruptibility.

## Consumers (corrects the brief)
Chokepoint `_cap_caller_scan_files()` (`repo_map.py:1530-1567`, early-return under ceiling `:1557`, slice `:1567`). Exactly 2 DIRECT callers: `build_symbol_callers_from_map` (`:14601-14606`) + `build_symbol_refs_from_map` (`:13962-13967`). `build_symbol_blast_radius_from_map` = 1-hop (via its callers call `:15287`). `build_symbol_impact_from_map` = NOT a consumer (map-universe-bounded, deadline-only). **4th consumer: `build_file_importers_from_map`** (`tg importers`, `:14455-14520`) re-implements the cap INLINE (`:14484-14485`) — no F1 ordering, no test, + a `result_incomplete` inconsistency (sets `caller_scan_limit` `:14501` but never calls `_mark_result_incomplete`; the exit-2 gate `main.py:8352` reads caller_scan_limit directly so the contract holds, but the shape is inconsistent). Derived sibling `CALLER_SCAN_ORDER_PROBE_CEILING = 4*CEILING` (`:178`) auto-scales.

## Safety preconditions (both hold, with caveats to gate)
- **(a) Parse-cache (#432):** `_parsed_source_and_tree_bounded` `@_mtime_aware_cache(maxsize=_PARSE_PRODUCT_CACHE_MAXSIZE)` (`:1288`). SHARED for JS/TS/Rust (`_js_ts_references_and_calls` :4402, `_rust_references_and_calls` :4792 via `_parsed_source_and_tree`). **Python is NOT cached** — `_python_references_and_calls` (:14666) does uncached `ast.parse` (:4060); real gap but ast.parse is cheaper (lower severity). **CRITICAL: `_PARSE_PRODUCT_CACHE_MAXSIZE = 1024` (`:1240`) < the new 2000 ceiling** → FIFO eviction (`:120`, insertion-order not LRU) thrashes the shared-parse guarantee on >1024-eligible-file repos. **MUST raise to 2048 in the same PR.**
- **(b) Deadline threading (#478):** the caller-scan main loop checks deadline every iter (`:14659`); all #478 sibling loops share `deadline_monotonic`/`_DeadlineBreakFlag`. Confirmed. So a larger scan is interruptible when `--deadline` is passed.

## MANDATORY companion changes (same PR — each closes a precondition gap)
1. Raise `_PARSE_PRODUCT_CACHE_MAXSIZE` 1024→2048 (`:1240`) — else precondition (a) is only partially true at 2000.
2. Retire the now-stale comment `:1547-1549` ("ceiling left at 512 on purpose — raising reintroduces #52's ~100s TS-regex hang") — #478 closed that; the comment now contradicts the code.
3. `build_file_importers_from_map` inline cap (`:14484`) auto-picks up the new constant — call it out (no test today).
4. Fix the `build_file_importers_from_map` `result_incomplete` inconsistency (§1) while touching it.

## Gate (golden-parity + latency + truncation — the ranking-fragility scar)
- **Golden-parity (superset, NOT byte-identical):** ≤512 repos byte-identical (early-return unchanged). (512,2000] repos = intentional SUPERSET — every old-512 caller still found in same order + newly-reachable callers now appear, no `result_incomplete` for a repo now fitting under 2000. NEW test: ~805-file repo (the dogfood shape) with a caller past index 512 → present + not-incomplete post-raise (absent today). 
- **Governance pins that WILL break (update, not delete):** `test_cap_fix_chokepoint.py:91-99` (hard-asserts CEILING==512); `:367-397` (`test_ordering_probe_bounded...` builds 3000 files, asserts `>PROBE_CEILING`=2048 — at 2000 ceiling PROBE_CEILING=8000, 3005>8000 FALSE → breaks; fix via monkeypatch-down the constant, per `test_parse_product_cache.py:335` precedent, don't inflate the fixture to 9000 files).
- **Latency (MANDATORY real-repo measurement, no speed claim without numbers):** `Measure-Command { tg callers <real-large-TS-repo> Node --deadline 10 --json }` before/after AND with `--deadline` OMITTED (the unprotected default path). + a CI-safe parse-spy test at ~2000 eligible files proving "≤1 real parse/file at the new ceiling" + a LOOSE wall-clock smoke (`<30.0s`, learn from #444's 1.0s-too-tight flake → start generous).
- **Truncation past the NEW ceiling:** extend the `bounds_scan_to_ceiling` tests (`:138-215`) with `--max-repo-files 2500+` companions asserting `result_incomplete`/`caller_scan_limit`/exit-2 still fire at 2000. NEW test for `build_file_importers_from_map`'s ceiling branch (zero coverage today).

## Build order (each red→green; `uv run --no-sync` gate)
1. RED-verify the 2 governance pins break as predicted. 2. Bump CEILING→2000 + update the pin + fix the PROBE_CEILING test (monkeypatch) + retire the stale comment. 3. Bump cache maxsize→2048 + cache-capacity regression test. 4. The ~805-file dogfood golden test. 5. The importers ceiling test + the result_incomplete fix. 6. Past-2000 truncation companions. 7. Real-repo Measure-Command (both --deadline + no-flag) BEFORE merge. 8. Full sweep (test_cap_fix_chokepoint, test_parse_product_cache, test_repo_map_deadline, test_file_deps, test_repo_map_graph + the #478 731-test backstop) in the REAL venv.

## Risks
Ranking-fragility (by analogy not mechanism — `_order_caller_scan_candidates` :1463 is partition/interleave, no numeric scorer, so the flat-scorer scar's root cause doesn't apply; but for a >2000 `--max-repo-files` repo, WHICH subset lands in the window is still order-sensitive → the past-2000 truncation tests mitigate). Latency on the flag-less default path (~4x worst-case file count for 512-2000 repos — likely correct tradeoff, silently-wrong worse than slower, but MUST quantify). Python precondition gap unmeasured (include a Python-heavy timing case). Out-of-scope (flag, don't build): a non-None default `--deadline` on graph commands = overlaps #94.
