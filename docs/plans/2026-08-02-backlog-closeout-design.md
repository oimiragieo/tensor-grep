# 2026-08-02 Backlog Closeout Campaign Design

**Status:** APPROVED FOR TASK 2A EXECUTION — Round 60 folds nine architecture/security/TDD blockers into the canonical plan: protected cryptographic installer authority, transacted-registry PATH mutation plus directory identity, production Microsoft-root trust, non-breakaway Jobs, all-front-door matcher accounting with combined inclusive limits, and independently re-derived current-run CI receipts. Cursor Auto's contradiction loop, independent TDD, and Sol cleared the status-stamped pair; final raw hashes are recorded outside these self-referential plan bytes in `MEMORY.md` and the dated CEO audit. Any further plan edit invalidates approval.
**Branch:** `campaign/backlog-closeout-2026-08-02`
**Scope:** every AI-actionable backlog item, open bug, and tracker contradiction; CEO-, hardware-, publication-, and spend-gated items become explicit decision records and are not autonomously executed.

## 1. Objective

Close the live backlog without treating historical prose as current truth and without bundling several independent product programs into one unreviewable change. At campaign completion every discovered item must be in exactly one state:

1. `SHIPPED` with a merged-commit and published-artifact receipt;
2. `RETIRED` with current contract/source evidence and a re-open trigger;
3. `BLOCKED` with a reproducible external prerequisite and an owner;
4. `CEO-GATED` with the decision and financial/publication consequence stated; or
5. `ACTIONABLE` with an approved, dependency-ordered implementation plan.

No item may remain merely “open,” “maybe fixed,” or “investigate later.”

## 2. Ground truth at design time

- The Round 18 design snapshot had `main` at `8fc51f8448cae6261235d30e3164843ee088d460` with main CI run `30765407062` successful.
- GitHub release and PyPI both serve `v1.102.1` / `1.102.1`.
- PR #910 has since merged as `8024125612d5fb42481acde34d94ad39bbaa3c3e`; latest exact main CI run `30793797849` completed successfully on that SHA. PR #911 is now the sole open PR; its old committed head is green, while the newer approved Round-60 plan bytes remain unpushed with exact-head CI pending.
- GitHub issue #48 is the only open issue and remains CEO-gated.
- `docs/TASK_BOARD.md` is the operational queue, GitHub is authoritative for PR/issue state, and `docs/BACKLOG.md` is evidence/history plus the August 2 dogfood findings. Dispatch always re-derives all three.
- Task 2's bounded current probes changed #89 and #90 to `READY`: the Windows-native executable fails on raw WSL absolute paths, while the same executable succeeds when only the typed path is translated with `wslpath -w`. The #90 treatment silently returned a clear scan (`matched_rules=0`) while the translated control found six matches. These receipts invalidate the prior non-defect retirement premise.

## 3. Approaches considered

### A. One branch containing the whole backlog

This produces one very large review surface across MCP, CLI contracts, evidence, language parsers, Rust routing, and multi-root orchestration. It also makes release attribution and rollback ambiguous and conflicts with the repository's one-release-per-publish discipline.

### B. Evidence-first sequential campaign — selected

First repair tracker truth, then close small contract/security residue, then deliver independent product programs in dependency order. Every release-affecting slice gets its own branch, PR, CI proof, adversarial review, merge, and published-wheel dogfood receipt. Shared-file language changes drain sequentially and union their assertions after each rebase.

### C. Tracker-only closeout

This would accurately retire stale work but leave the actionable MCP disclosure, atomic-writer ratchet, edit verification, language depth, and multi-root work unfinished.

## 4. External research and resulting constraints

The Round 1–18 research record was run with Exa on 2026-08-02 and restricted to official specifications, project documentation, and source repositories. The requested Exa connector was not exposed in the Task 2 amendment runtime, so the WSL amendment used current official Microsoft primary documentation directly rather than claiming an Exa result it could not obtain.

### WSL interoperability and path domains

Sources:

- https://learn.microsoft.com/en-us/windows/dev-environment/wsl-interop
- https://learn.microsoft.com/en-us/windows/wsl/release-notes
- https://learn.microsoft.com/en-us/windows/wsl/filesystems

Current Microsoft documentation confirms that WSL can invoke Windows executables, `WSLENV` with the `/w` flag transports a WSL path to a Windows process, `wslpath -w` converts a Linux path to Windows form, and the WSL automount root is configurable. The live treatment/control additionally proved that ordinary exported custom variables did not reach the Windows executable until explicitly named in `WSLENV`.

Design consequences:

- The WSL-installed launcher transports explicit caller-domain provenance through `WSLENV`; it does not rewrite arbitrary argv in shell.
- Translation recognizes typed absolute POSIX filesystem fields, not `/mnt` string prefixes, because the automount root is configurable.
- The Windows process invokes the fixed `wsl.exe`/`wslpath` argv without a shell, validates and bounds the returned Windows path, and fails closed on ambiguity or timeout.
- Patterns, replacements, inline YAML, globs, filters, language names, justifications, and validation commands remain opaque data even when they resemble paths.

### MCP discovery

The current MCP specification makes capability discovery and version negotiation explicit, with server capabilities and supported versions available before ordinary calls. Tensor-grep's own `tg_mcp_capabilities` is therefore the correct additive location for a `tool_surface` disclosure; changing the default advertised set is a separate compatibility decision.

Sources:

- https://modelcontextprotocol.io/specification/2026-07-28/server/discover
- https://modelcontextprotocol.io/specification/2026-07-28/basic/versioning
- https://modelcontextprotocol.io/specification/2025-06-18/server/tools
- https://github.com/github/github-mcp-server

Design consequences:

- Add `tool_surface: "full" | "lean"` to `tg_mcp_capabilities`.
- Keep `TG_MCP_LEGACY_TOOLS` default-on.
- Task 2C bumps the MCP contract from `1.7.0` to `1.8.0` for WSL path-domain mappings; MCP tool-surface disclosure bumps `1.8.0` to `1.9.0`; Task 13's workspace tool bumps `1.9.0` to `1.10.0`; only a value-surviving Task 14 graph action adds `tg_graph` and bumps `1.10.0` to `1.11.0`.
- Test both import-time flag states in subprocesses and the real stdio protocol.
- Do not represent `tool_surface` as a core MCP protocol version or imply that clients negotiated it.

### Controlled edit workflows

Current tools separate pre-edit intent/scope from deterministic post-edit verification, compare the actual patch with declared boundaries, and emit a receipt. They also distinguish a graph-risk signal from proof of runtime correctness.

Sources:

- https://orenlab.github.io/codeclone/guides/agent-safe-change/
- https://github.com/orenlab/codeclone
- https://github.com/vk0dev/code-impact-mcp

Design consequences:

- Implement `verify-edit` before `edit-ready` so the immutable comparison contract exists before orchestration.
- `verify-edit` is pure by default: it reads a baseline capsule and repository state but does not execute commands or mutate the ledger.
- `edit-ready` is not an alias for `prepare`. It adds a strict cooperative-readiness ticket with explicit named identity, successful claim, persisted baseline, complete analysis, and zero unresolved conflicts; it is not a security authorization.
- Blast-radius heuristics remain evidence, not a guarantee. Validation results stay a separate evidence family.

### Evidence and attestation

SLSA/in-toto verification binds evidence to a subject digest, verifier identity, policy, and expected source/repository identity. Unknown security-sensitive policy inputs should fail verification rather than be ignored.

Sources:

- https://slsa.dev/spec/v1.1/provenance
- https://slsa.dev/spec/v1.2/verifying-source
- https://slsa.dev/verification_summary

Design consequences:

- Edit receipts bind to `gitCommit` plus dirty-tree digest and canonical repository identity.
- The schema has an explicit major version and additive minor evolution rules.
- Verification records the exact policy/schema digest and rejects unknown required policy fields.
- A locally produced receipt is evidence of deterministic comparison, not an unsupported SLSA compliance claim.

### Parser-backed caller/reference extraction

Tree-sitter's code-navigation vocabulary distinguishes definition and reference roles, including `@reference.call`, and its generated `node-types.json` provides the grammar-specific node shapes needed to prevent guessed queries.

Sources:

- https://tree-sitter.github.io/tree-sitter/4-code-navigation.html
- https://tree-sitter.github.io/tree-sitter/using-parsers/queries/1-syntax.html
- https://github.com/tree-sitter/tree-sitter/blob/1ffd612b/docs/src/using-parsers/6-static-node-types.md

Design consequences:

- Each language wave begins with live AST-shape/node-type fixtures.
- Extract definitions, references, and calls as separate roles.
- Include same-named decoys and unsupported-shape honesty tests.
- Never infer cross-file import resolution from a same-named file alone.

### Graph coding and graph engineering

Exa research on 2026-08-02 found three distinct ideas sharing the current “graph coding” label:

- a persistent code/dependency knowledge graph for retrieval, impact, and traces;
- an editable workflow/control graph that coordinates specialized agents; and
- a typed causal event graph that supports run replay, forks, and projections.

Primary and current project sources:

- https://github.com/levi-qiao/graphkit
- https://github.com/abhigyanpatwari/GitNexus
- https://github.com/BUPT-GAMMA/MASFactory
- https://arxiv.org/abs/2603.06007
- https://activegraph.ai/blog/ag-coder
- https://www.langchain.com/blog/3-years-of-graph-engineering-with-langgraph
- https://github.com/abhigyanpatwari/GitNexus/issues/1821
- https://github.com/abhigyanpatwari/GitNexus/issues/1532
- https://github.com/abhigyanpatwari/GitNexus/issues/1858
- https://github.com/abhigyanpatwari/GitNexus/issues/567

The label is new and not a settled architecture standard. LangChain's current retrospective explicitly frames loops as simple cyclic graphs and reports that some open-ended research moved away from predefined graphs, while constrained multi-stage workflows benefit from explicit paths. The underlying user needs are nevertheless concrete: agents want a stable, versioned graph contract; one-call diff-to-impact context; honest unresolved-indirection disclosures; and bounded machine-readable evidence instead of repeated free-form search loops. Current issue reports also show demand for shortest symbol traces, but tensor-grep does not yet materialize a repository-wide call graph, and program-dependence graphs multiply index cost substantially. Those are evidence for a narrow first slice, not permission to overstate current graph completeness.

Design consequences:

- Tensor-grep provides a rebuildable code-graph projection and change-impact evidence. It remains a context/search appliance, not a workflow engine, task board, graph database, authorization system, event store, visual builder, or multi-agent supervisor.
- The first projection exposes only relationships supported by current indexed/parser evidence. Missing call/reference/import resolution is `query_only` or `unsupported`, never an omitted edge interpreted as zero.
- The first change-impact surface is read-only, single-repository, Git-diff-driven, deterministic, capped, and fail-closed on unsupported or truncated evidence.
- Arbitrary Cypher/Gremlin, rename/apply actions, graph-authored directives, event replay/fork, cross-repository graphs, program-dependence graphs, and LLM-generated topology are deferred behind measured demand and separate reviewed plans.
- External projects are research inputs and interoperability targets, not new runtime dependencies.

## 5. Campaign architecture

### Program 0 — tracker reconciliation

Drain #910 after its own independent review. Rebase the campaign worktree on the resulting `origin/main`, then regenerate the live inventory. Close stale entries only with source, test, PR, and published-artifact receipts.

Known reconciliation decisions:

- F1/#22: retire as settled by `docs/CONTRACTS.md`; exit 2 means incomplete, not “complete but interpret carefully.”
- F2: retire the claim that outright anonymous refusal was never considered. `ledger_store.resolve_agent_id` records that it was considered and rejected for the legacy surface.
- #90: split the mixed historical item honestly. Its doctor exit-127 half shipped in PR #571, but the scan half is a current actionable path-domain and false-clear defect. Keep #90 `READY` until Tasks 2B/2C merge, publish, and close through a separate closure PR.
- #109: mark shipped by PR #605 and remove it from the hardware-blocked queue.
- #89: the bounded WSL reproduction failed on the raw WSL path and passed when only that typed path was translated for the same Windows-native executable. Keep it `READY` until Tasks 2A/2C merge, publish, and close through a separate closure PR.
- #36 and #37: mark shipped by #903 and #908.
- #859: reopen the original class-level atomic-writer ratchet; the codemap-specific test did not satisfy it.
- F5/F6/F7/F8 plus stable `MCP-SURFACE`, `CPU-BACKEND`, and `REF-CALL-REGISTRY` IDs own every Tasks 4–13 implementation program as `READY`; #77 is the sole canonical row owning the #77/F9 alias pair, while #48/#72/#77/#131/#169 remain the five CEO-gated rows. F10/MaxSim, DD-004, DD-006, full AST DSL parity, MCP lean-default, continuous refresh, and `RUST-REPLACE-SYMLINK` are stable demand-gated IDs, together with #255. The last ID owns the unresolved public Rust direct-leaf-symlink compatibility/security decision and prevents `CPU-BACKEND` from silently absorbing or dropping it. The canonical block has a unique machine-parsed handoff-version line and an exact closed-world ID/status set shared with `SESSION_HANDOFF.md`. Each implementation row moves `READY` → `IN_FLIGHT` only when its PR exists, then a separate gated/merged/post-merge-verified closure change preserves the implementation PR, records the closure PR separately, and moves it to `SHIPPED`; no code PR certifies its own future merge.

### Program 0A — atomic WSL path-domain closeout (#89/#90)

The reproduced defects share one architectural cause: a WSL launcher selects a Windows-native executable but passes Linux-domain filesystem arguments through unchanged. The fix is one typed boundary program, not blanket argv rewriting.

The generated WSL launcher reserves `TENSOR_GREP_CALLER_PATH_DOMAIN` and `TENSOR_GREP_CALLER_WSL_DISTRO`. Unset or exactly empty `WSLENV` is zero entries. Otherwise, a token is exactly nonempty environment name `[A-Za-z_][A-Za-z0-9_]*` plus optional `/` and a nonrepeating subset of `u,w,p,l`; leading/trailing/interior empty tokens, unknown/repeated flags, or invalid names are malformed. The launcher parses at most 256 entries and 32 KiB of input, removes every reserved-name entry case-insensitively, preserves unrelated entries byte-for-byte/in-order, appends exactly one canonical `NAME/w` entry for each reserved name, and enforces 32 KiB again on final rebuilt UTF-8 bytes. All Bash byte counts and indexing run under `LC_ALL=C`; locale-dependent character counts cannot satisfy byte caps. It explicitly exports the result. `WSL_DISTRO_NAME` is nonempty valid UTF-8 of at most 256 bytes with no control; cap−1/cap/cap+1 are pinned. Embedded NUL is process-ABI-unrepresentable and is rejected only by the shared pure byte-parser vectors, not claimed as an executable shell-environment fixture. Any representable malformed/overflow/distro failure exits exactly 2, writes only `tg: invalid WSL launcher environment; native command not started` plus LF, and never execs. The shim overwrites/exports caller domain plus validated distro, then `exec`s original `"$@"` byte-for-byte. A shim resolves sibling `tg.exe` only when that exact directory received and hash-verified the tensor-grep-managed native copy during the same install/upgrade transaction. Installer ownership proof is a bounded, duplicate-key-rejecting `InstallerShimReceiptV1` that binds release tag plus live `CHECKSUMS` asset SHA-256, install-command digest, canonical managed-directory identity, protected installer-state identity, selected release-asset identity, and exact known generated-shim byte digest. Before any owned leaf mutation, the installer validates the protected signed receipt plus retained no-follow state/directory/shim/binary identities and selects only an opened-identity-matched PATH token. PATH mutation then uses the Round-60 TxR transaction; if TxR is unsupported, a concurrent non-transacted writer interferes, or commit fails, the installer fails closed with zero visible shim, binary, receipt, or PATH change and never falls back to read/compare/write CAS. Corrupt, duplicate, stale, forged, cross-tag, reparse, or alias authority fails closed. If a previously managed sibling `tg.exe` becomes foreign, upgrade preserves that foreign executable and every genuinely foreign/unowned shim, but atomically replaces the tensor-grep-owned sibling-resolving shim with a non-executing fail-closed tombstone and removes that directory from tensor-grep-managed PATH routing before completing; the managed primary directory receives the usable shim. A clean directory containing a foreign `tg.exe` receives no shim. The launcher never depends on the installer's hard-coded `/mnt/<drive>` conversion; any Python fallback uses a separately bounded/configurable-automount conversion. Fresh install and upgrade tests use a non-`/mnt` automount root and prove the launcher reaches the selected binary before any operand bridge runs; clean-foreign and managed-shim→foreign-executable arms prove neither direct old-shim nor PATH invocation executes the canary while the new primary shim works. Native code treats both provenance variables absent as legacy only outside this installed shim; partial/malformed provenance is exit 2, never raw fallback.

The path bridge obtains System32 through `GetSystemDirectoryW` rather than `SystemRoot`, retains a no-reparse System32 directory handle, opens regular non-reparse `wsl.exe` relative to that identity with `FILE_SHARE_READ` only (no write/delete sharing), and keeps both exact handles open from identity capture through trust and child-image comparison. Final-path text is optional corroboration only and never accepts or rejects a candidate without the retained parent/leaf identities. Trust has two explicit handle-bound branches. Embedded signing evaluates the held `WINTRUST_FILE_INFO.hFile`; catalog signing calculates the member hash from that same held handle, enumerates the local catalog store, and evaluates each candidate with `WTD_CHOICE_CATALOG` plus `WINTRUST_CATALOG_INFO.hMemberFile` bound to that handle. Both use `WinVerifyTrust(WINTRUST_ACTION_GENERIC_VERIFY_V2)` with `WTD_UI_NONE`, `WTD_REVOKE_WHOLECHAIN`, and `WTD_CACHE_ONLY_URL_RETRIEVAL | WTD_REVOCATION_CHECK_CHAIN_EXCLUDE_ROOT`, and accept only a verified candidate whose member hash still matches. The acceptance predicate requires leaf Code Signing EKU `1.3.6.1.5.5.7.3.3`, time validity, `CERT_CHAIN_POLICY_MICROSOFT_ROOT` with test-root acceptance disabled, and a terminal-root SHA-256 in the maintained production Microsoft allowlist. Normalized Organization is corroboration only; a user-trusted foreign leaf/root pair claiming `Microsoft Corporation`, a non-Microsoft production root, untrusted catalog, catalog member-hash mismatch, cold-cache network canary, cleared cache-only flag, or dual swap fails. The real local System32 catalog-signed `wsl.exe` is a treatment; a same-shape non-System32/canary file is the control. It invokes that absolute executable with argv `--distribution DISTRO --exec /usr/bin/wslpath -w -- PATH`, never a shell. Python and Rust both use low-level `CreateProcessW` with a non-null exact application name, mutation-safe command buffer whose decoding is round-tripped against shared argv vectors, `CREATE_SUSPENDED` without `CREATE_BREAKAWAY_FROM_JOB`, and an explicit inherited-handle allowlist; ordinary `Popen`, `Command::spawn`, `taskkill`, or a reopened-path proxy cannot satisfy this boundary. The adapter retains `PROCESS_INFORMATION`, assigns the suspended child before resume to a Job with `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` and neither `JOB_OBJECT_LIMIT_BREAKAWAY_OK` nor `JOB_OBJECT_LIMIT_SILENT_BREAKAWAY_OK`, queries the actual child image, and compares its volume/file identity to the still-open verified original handle. Whether `CreateProcessW` returned process/thread handles—not the reason code—defines the cleanup phase. Every later failure, including Job assignment, resume, child-image query, pipe-worker initialization, timeout, exit, decode, output, or identity, terminates and boundedly reaps the child handles; after successful Job assignment, closing/terminating the Job also kills descendants, and a real breakaway attempt must leave no surviving canary. A Job-assignment failure terminates the still-suspended primary process directly before reaping. Stdout and stderr are drained concurrently through `cap+1`. Per child is 2 seconds, aggregate owner budget is 10 seconds, each stream is 32 KiB inclusive, and one owner accepts at most 256 distinct paths. Input is absolute POSIX and at most 16 KiB. Output accepts valid UTF-8 containing exactly one path plus one optional terminal LF/CRLF: either drive-absolute or ordinary UNC. It rejects embedded CR/LF, drive-relative/root-relative forms, ADS, DOS reserved devices, and `\\.\\`/`\\?\\`/NT namespaces. Identity/confinement is still rechecked at the target use; output grammar is not authorization. Rust adds a narrowly featured target-Windows direct `windows-sys` dependency pinned through `rust_core/Cargo.toml`/`Cargo.lock`; all unsafe APIs and RAII handle ownership stay inside `path_domain.rs`. Python lazily imports the low-level adapter so bootstrap fast-path import guarantees remain pinned.

Python and Rust implement one versioned `PathDomainContractV1` against one committed conformance-vector corpus. The corpus pins exact accepted/rejected inputs, `WSLENV` normalization, limits, caching, and the exhaustive translator reason set `invalid_provenance | input_limit | translation_limit | bridge_unavailable | bridge_timeout | bridge_output_limit | bridge_failed | bridge_decode_error | invalid_translated_path`. Provenance shape maps to `invalid_provenance`; distro/path size or grammar to `input_limit`; the distinct-count cap to `translation_limit`; Windows API/executable/signature/job setup to `bridge_unavailable`; elapsed deadlines to `bridge_timeout`; either stream cap to `bridge_output_limit`; nonzero exit or post-create identity change to `bridge_failed`; invalid UTF-8 to `bridge_decode_error`; and decoded path grammar failure to `invalid_translated_path`. Each public request has exactly one translation owner. The invariant order is: lexically enumerate and size/grammar-validate the complete typed-path set in caller-domain bytes; select the execution target domain; for a Windows target translate every distinct operand under the one request budget; rewrite only the typed slots and validate/set the consumed marker; only then permit target-domain existence checks, pattern/ignore reads, generated-ignore discovery, request resolution, or downstream spawn. A deliberately selected WSL-local route retains raw caller-domain values and a local-domain state rather than setting the Windows consumed marker. Direct native search/run performs the same sequence in Rust; native scan forwards the complete original scan argv to the Python sidecar, which owns the sequence. A consumed marker is accepted only when no absolute POSIX typed field remains; otherwise exit 2. No route splits fields or resets the 256-path/10-second budget across Rust and Python.

`PathDomainEvidenceV1` is exact: `{version:1, caller:"wsl"|null, target:"windows", status:"translated"|"error", reason:null|PathDomainReason}`; `reason` is null iff status is translated. It is absent only when both provenance variables are absent. Tensor-grep aggregate JSON search/scan/run and MCP envelopes add exactly one `path_domain` object; a failure also carries `result_incomplete=true`, `incomplete_reason_class="path_domain_mismatch"`, the existing route's complete key set, and exit 2 (or a returned MCP error envelope, never an exception-only/empty response). Cross-domain tensor-grep-owned NDJSON emits match rows without duplicated provenance and exactly one terminal metadata row containing `path_domain`, including zero-match; same-domain NDJSON remains byte-identical. Text errors lead with one sanitized incomplete-result line; SARIF sets `executionSuccessful=false` and the same object. Success/no-match keep exits 0/1. The only raw-ripgrep JSON-lines surface is `--format rg --json`; it and ordinary successful text stay byte-identical and omit tensor-grep provenance. Contract/docs/tests pin every mapping and subreason.

Task 2A covers only read-only bootstrap/full/direct-native `tg search`: positional roots plus pattern-file `-f/--file` and `--ignore-file` operands in the exact public spelling census. Unreachable spellings first receive a routing/ownership RED and behaviorless registration green; only then may their raw-operand failure count as the semantic translation RED. The existing bootstrap/full native-delegation refusal for pattern/ignore files is a correctness quarantine, not dead code: it may open only after Rust implements and pins real pattern-file loading, multi-pattern result parity, explicit ignore-file forwarding, generated root-ignore handling, and Python-sidecar nonuse for the native-owned route. Each pattern/ignore file is at most 1 MiB, at most 32 such files and 4 MiB aggregate decoded bytes exist per request, each pattern and ignore rule is at most 16 KiB, the combined positional/`-e`/`-f` set is at most 65,536 patterns, and all explicit plus generated ignore inputs contribute to one 65,536-rule aggregate cap. Reads and rule splitting stream through `cap+1`, require strict UTF-8 and no NUL, and never sample a prefix as complete. Every selected matcher engine must expose chargeable construction and match-loop operations to `SearchInputLedger`; an engine that cannot prove those charges (including an uninstrumented backtracking/PCRE2 route) is hard-refused with `incomplete_reason_class="search_input_limit"` before compilation. Instrumented matcher compilation and per-route pattern/ignore matching stop before transition cap+1 with no downstream effect; matcher output is only emitted after both construction and matching loops complete within `SearchInputLedger`'s 300-second request-entry deadline; the bridge's separate 10-second aggregate applies only to translation. A committed real-rg-derived golden table freezes empty file, LF/CRLF, blank line, trailing terminator, duplicates, missing/unreadable, invalid UTF-8/NUL, and mixed positional/`-e`/`-f` ordering; pattern and ignore count/length cap−1/cap/cap+1, mixed-pattern+ignore stream cap−1/cap/cap+1, and over-byte-limit or invalid input yield the full structured envelope with `incomplete_reason_class="search_input_limit"` and exit 2 before matcher construction or downstream search. The exact domain sequence above completes before root existence checks, pattern/ignore-file reads, generated-ignore discovery, or any downstream child. Exact registered forms are `-f P`, `--file P`, `-fP`, `--file=P`, `-ifP`, `-if P`, `--ignore-file P`, and `--ignore-file=P`; `-f=PATH` means an attached filename beginning `=`, and `--ignore-filePATH` is invalid. Patterns and other option values remain opaque. Task 2B covers every typed `tg scan` filesystem field: positional scan roots, `--config`, `--rule`, ruleset `--path`, baseline/suppression reads, and baseline/suppression writes. It does not translate ruleset names, inline YAML, filters, languages, justifications, globs, types, or evidence text. Task 2B introduces exact shared `_index_lock.atomic_write_bytes_anchored(path, data, *, mode=None, replace: bool)` plus its anchoring test module. It first characterizes each scan write option's regular-file overwrite/no-clobber behavior and preserves it with `replace`; directories and link/reparse destinations remain refused. Task 3 consumes and expands this already-shipped helper. A missing explicit root is incomplete/exit 2 rather than an empty iterator. Task 2C closes mutation/security twins: Python/Typer `tg run` owns only its positional root; direct-native `tg run` owns root plus `batch_rewrite`, `audit_manifest`, and `audit_signing_key`; MCP owns `tg_index_search`; `tg_query(action="index")` with `path` and every `workspace_roots[]`; direct `tg_rewrite_plan/apply/diff`; and `tg_rewrite(action="plan|apply|diff")` path, policy, audit-manifest, and signing-key fields. Indexed search is mutation-capable because it may create/rebuild `.tg_index` and receives the same no-effect/atomic-publication gate as rewrite.

MCP performs only grammar, provenance, and size preflight in the caller domain. It then translates, canonicalizes with no-follow target handles, enforces `_mcp_root` and the complete operation/rewrite policy on every target-domain identity, rejects canonical read/write and multi-field aliases, and retains/revalidates handles across use. Index creation completes authorization before state creation and uses the existing per-root lock/atomic publication contract. Signing-key/policy reads are bounded regular-file reads through identity-verified no-follow handles; audit publication is handle-relative and cannot alias a key/policy/root. Event-gated swaps between translation, authorization, read, and publication fail closed. Errors and receipts never expose raw user/distro/path/key values or child stderr.

Tasks 6–8 and 12–13 must extend the same registry, vectors, mutation gates, and public WSL dogfood for every new filesystem field before shipping. Evidence emit's complete Python-owned set is positional `REPO`, `--manifest`, `--capsule`, `--cost-json` (including transported `TG_EVIDENCE_COST_JSON`), explicit/transported `--signing-key` (`TG_EVIDENCE_SIGNING_KEY`; its target-created default remains target-domain), `--previous`, `--out`, and `--edit-verification FILE`; only edit-verification `-` is an opaque stdin sentinel. Query/model/agent/checkpoint IDs remain opaque. Verify-edit types `REPO` and `--validation-file`; baseline `NAME` is opaque. Edit-ready types `REPO` and validation file, while editable/review-only scopes remain required repo-relative opaque values (absolute WSL or Windows scope values stay invalid) and `--out NAME` remains opaque. Workspace-prepare types `ANCHOR`/`--root`; MCP workspace types anchor/roots. Every task declares its Python/Rust/MCP owner; an unregistered typed field fails the closed-world census.

Deterministic tests inject process/clock/handle adapters and pin the complete field and owner matrices, opaque controls, shared vectors, duplicate caching, limits, trusted executable resolution, process-tree cleanup, invalid output, configured non-`/mnt` automount, absent/partial provenance, and exact envelopes. A lexical/provenance/limit/trust failure before `CreateProcessW` returns handles has zero bridge child, zero downstream search/rg/native/sidecar child, and zero protected target-data read/write. Every failure after handles return—including Job assignment, resume, image query, or pipe-worker setup—permits the one failing bounded bridge child (plus any earlier successful distinct translations), requires it and any descendants killed and boundedly reaped, and still has zero downstream child and zero protected target-data read/write; executable metadata/signature reads are not target-data reads. Reason mapping stays independent of this resource-cleanup classification. Unchanged public tests are mutation-proved by removing one production mapping/guard at a time. Mandatory Windows/native CI compares a committed exact node manifest (with owning workflow/job names, required runner class, command digest, and non-skip disposition—but no live run/attempt ID) to current-run-bound dedicated executed non-skipped receipts: a pinned Python runner emits both JUnit and strict `NativeCiReceiptV1` for only its manifest-owned invocation, while a pinned stable-Rust runner first proves each fully qualified node occurs exactly once in the selected test binary and then executes it with `--exact --include-ignored`, emitting a versioned TG receipt. The ordinary complete Rust suite remains a separate required job and is not compared as a closed-world Task 2A set; live WSL is separate dogfood. #89 owns Tasks 2A/2C and #90 owns Tasks 2B/2C. Published proof uses the exact tagged installer/release asset (URL and SHA-256), a clean fresh install and managed upgrade, the generated shim, and the selected binary version/hash—not a source-tree/global shim or wheel alone.

Round-59 Task 2A addenda are load-bearing refinements of the preceding Program 0A contract. `InstallerShimReceiptV1` is bounded and duplicate-key-rejecting; it binds the release tag plus that tag's live `CHECKSUMS` asset SHA-256, install-command digest, canonical managed-directory identity, protected installer-state identity, selected release-asset identity, and exact known generated-shim byte digest. The install-command digest is audit metadata only and never authority. Receipt authority is discovered only from a fixed, retained, no-follow installer-state root created beneath the system ProgramData identity with an explicit Administrators/SYSTEM-only security descriptor; PATH, the managed binary directory, environment variables, and caller-provided paths never discover or replace it. A non-exportable CNG signing key with the same restricted ACL signs the canonical receipt bytes, and the verifier binds its public-key thumbprint and key name to that protected state. A planted receipt anywhere outside that retained state root, an unsigned receipt, a changed public-key binding, or a valid receipt moved from another installation is ignored and fails closed. The installer retains no-follow receipt, managed-directory, shim, and binary handles and stages replacement/tombstone/receipt bytes without changing visible leaves. PATH is mutated only through an explicit TxR transaction (`CreateTransaction` + transacted registry open/write + `CommitTransaction`); no process lock, read/compare/write emulation, or non-transacted fallback is called atomic. A pre-commit non-transacted competing write makes commit fail/roll back. The protected, bounded, fsynced `InstallerTxnV1` journal coordinates the registry transaction with handle-relative atomic leaf replacements while retaining recoverable old leaves. Any injected later failure conditionally restores the exact PATH preimage and old leaves; startup recovery idempotently reaches the complete old or complete new state without overwriting unrelated concurrent PATH edits. Before removing a PATH token, the installer canonicalizes its case, extended-path, short-name, and separator aliases, opens the directory with no-follow semantics, and requires its volume/file identity to equal the still-retained receipt-owned managed directory; a junction/reparse alias is never removable authority. Corrupt, duplicate, stale, forged, cross-tag, reparse, case/8.3/`\\?\\`/junction alias, post-hash leaf/parent-swap, Event-gated PATH-conflict, outside-state planted receipt, and crash-at-every-journal-phase fixtures fail closed with foreign files unchanged and prove that no completed transaction exposes a mixed old/new state.

The bridge additionally retains a no-reparse System32 directory handle and opens `wsl.exe` relative to that identity. Event-gated directory/junction swaps between `GetSystemDirectoryW`, directory open, leaf open, and the post-create identity comparison fail closed; a final-path string without the retained parent identity is not authority. Both embedded and catalog trust arms set `dwUIChoice=WTD_UI_NONE`, `fdwRevocationChecks=WTD_REVOKE_WHOLECHAIN`, and `dwProvFlags=WTD_CACHE_ONLY_URL_RETRIEVAL | WTD_REVOCATION_CHECK_CHAIN_EXCLUDE_ROOT`; clearing the cache-only flag must trip the network canary. The verified chain must also pass `CERT_CHAIN_POLICY_MICROSOFT_ROOT` with test-root acceptance disabled and terminate at a production Microsoft root whose SHA-256 thumbprint is in the maintained allowlist. Organization text is corroboration only: a user-trusted foreign root and leaf that both claim `Microsoft Corporation` is a mandatory RED. The Job Object sets `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`, sets neither `JOB_OBJECT_LIMIT_BREAKAWAY_OK` nor `JOB_OBJECT_LIMIT_SILENT_BREAKAWAY_OK`, and child creation excludes `CREATE_BREAKAWAY_FROM_JOB`; a descendant breakaway attempt is a mandatory RED whose canary cannot survive closing the Job.

One no-refund `SearchInputLedger` spans explicit and generated sources across Python/Rust and child delegation. In addition to the file/byte/rule limits above, it caps compiled matcher live memory at 64 MiB and non-backtracking matcher transitions at 10,000,000. Its fixed 300-second absolute safety deadline starts at public-request entry and covers pre-open generated-ignore discovery, open, decode, normalize, compile, and matching. Bootstrap, full CLI, direct native, and every native-to-rg/native-to-sidecar delegation gate must prove the same ledger is installed before route selection; an uninstrumented PCRE2 or downstream engine is refused before any native, rg, sidecar, or matcher child starts. Every numeric dimension receives independent cap−1/cap/cap+1 plus mixed explicit/generated fixtures: each 1 MiB file, the combined pattern+ignore 32-file and 4 MiB decoded-byte totals, the combined positional/`-e`/`-f` pattern total, and the explicit+generated ignore-rule total. Separate counters or an off-by-one rejection at the inclusive cap must fail unchanged tests. Over-cap or deadline exhaustion emits the route's full structured incomplete envelope with the literal `incomplete_reason_class="search_input_limit"` and exit 2 before the next allocation/transition or downstream search.

Every dedicated Windows/native record uses strict `NativeCiReceiptV1`, created only in an empty current-run directory. It binds the live manifest SHA-256, exact commit, workflow run ID/attempt/job, deterministic runner-identity SHA-256, binary path/version/identity with equal pre/post SHA-256, sorted unique node list plus node-census artifact digest (pytest JUnit or Rust list output), exact argv digest, captured output digest, and exit digest. The final verifier does not trust those identity fields merely because the JSON says them: it independently derives repository, commit SHA, workflow run ID, attempt, job, runner identity, and artifact namespace from the live GitHub Actions environment plus the current artifact-download context, then cross-checks the receipt, JUnit node population, Rust `--list` census, command digest, and binary identity against that external tuple. It reads only that current-run artifact namespace and rejects a seeded directory, duplicate binding, cross-attempt replay, missing/wildcard binding, environment/receipt disagreement, manifest/binary/node/argv/output/exit drift, or source-tree evidence labeled as wheel proof. Published installer proof also requires the selected asset digest to match that tag's live `CHECKSUMS`.

Round-60 precedence is exact. Its protected signed `InstallerShimReceiptV1` authority, transacted-registry PATH mutation and opened-directory identity replace—not extend—the earlier release-URL/parent/shim-tuple and read/compare/write shorthand. A receipt missing tag/`CHECKSUMS`, protected-state/CNG binding, canonical-directory, selected-asset, or known generated-byte identity is invalid. The retained no-reparse System32 directory handle and relative leaf open are authority; the final-path string is corroboration only. Exact offline WinTrust/Microsoft-root and non-breakaway Job rules override earlier Organization-only/kill-on-close shorthand. The 2-second-child/10-second-aggregate budget is translation-only, while `SearchInputLedger` owns the separate 300-second request-entry-through-match deadline at every front door. Every ledger cap/deadline exhaustion uses the literal `incomplete_reason_class="search_input_limit"` in the route's full incomplete envelope; no synonym or free text is allowed.

### Program 1 — concrete security and MCP residue

#### Atomic CLI writer ratchet

Create an AST-based census test for CLI functions that publish user-facing artifacts. The census detects direct write-to-temporary-then-publish patterns and direct destination writes, proves the detector on the exact unmodified pre-#869 codemap blob, discovers every generated-Python execution root from production spawn callsites, fails closed on dynamic/unparseable payloads, and pins the complete production population through a lexical-scope-aware resolver plus an independent raw-call inventory. Generated source and sanctions use exact source/callsite/operation/destination-provenance fingerprints; a whole-function exemption is forbidden. Direct or aliased `.resolve()`/`realpath()` of a caller-selected leaf before an approved helper is itself a violation, not helper-backed. Approved shared atomic helpers are the normal route. The live deep-dive found three production violations to drive genuine REDs: `_write_json_refuse_symlink` (including caller-side pre-resolution that erases leaf identity), `_write_ast_project_scaffold`, and `new`.

Route their caller-selected JSON/YAML artifacts through shared parent-handle-anchored writers and preserve each site's existing regular-file semantics: refresh/ruleset artifacts may overwrite, while `new` and scaffold `sgconfig.yml` remain atomic create-if-absent/no-clobber. POSIX directory creation and publication are directory-fd-relative; Windows uses an opened no-reparse ancestor plus handle-relative directory-create/file-create/rename operations. If safe relative creation is unavailable, fail closed when a parent is absent. A path recheck followed by path-based mkdir/rename is not sufficient. Installer/runtime directory swaps remain separately enumerated because their replacement and rollback contract is not equivalent to a JSON/text artifact writer. Every artifact writer, including overwrite writers, refuses pre-existing live, dangling, or reparse leaf links rather than replacing or following their directory entries. Event-gated swaps before every directory-creation boundary plus late-leaf and parent-directory swap/junction tests must prove the complete external tree remains unchanged.

#### MCP tool-surface disclosure

Add `tool_surface` derived from the same import-time `TG_MCP_LEGACY_TOOLS` decision used to build both the live registry and capability map. The field and registry must never disagree. The default remains `full`; recognized off tokens produce `lean`. Contract, docs, unit tests, subprocess flag tests, stdio integration tests, and package version pins move together.

#### Backend surface decision

Rust `CpuBackend.replace_in_place` is already a public API: the public `backend_cpu` module is built into the crate's `rlib`, and both the type and method are public. Retain its exact `fn(&CpuBackend, &str, &str, &str, bool, bool) -> anyhow::Result<()>` signature with a compile-time external assertion, retain streaming traversal, and propagate directory-walk/literal/regex child errors with stable contextual messages. The public method must unconditionally delegate through the same private injectable core used by fault tests, so a disconnected test seam cannot create false evidence. Direct-file errors already propagate; nonexistent-path and direct-leaf-symlink semantics remain unchanged because they require a separate compatibility/security decision. An in-repository caller census still documents observed use, but cannot prove absence of downstream Rust consumers. Removal is outside this campaign unless separately approved as a breaking API change with deprecation and migration plans.

The Python `CPUBackend` has a separate A27 twin defect: two raw PyO3 search adapters retry without `invert_match` after any `TypeError`, unlike `RustCoreBackend`, which already retired that unsafe compatibility pattern. Route the inline simple path through `_rust_match_set`, reduce that helper to one exact-signature native call, and map an internal native `TypeError` to `BackendExecutionError` without a second call, dropped inversion, or fixed-string Python fallback. Genuine native absence retains its explicit import-error fallback. Retain `CPUBackend`, `RustCoreBackend`, and the PyO3 class; their contracts differ and consolidating the public classes would create a dependency cycle.

### Program 2 — deterministic edit verification

Introduce a shared internal module, `tensor_grep.cli.edit_verification`, rather than embedding more policy in `cli/main.py`.

#### `EditBaseline` schema

Version 1 contains:

- canonical repository identity and root;
- `gitCommit`, branch/ref when available, dirty flag, and dirty-tree digest;
- a bounded, canonical `preexisting_changes` manifest for every dirty/staged/untracked path at baseline time: porcelain status, current and original path for renames, object kind (`regular | symlink | deleted | other`), normalized worktree mode, byte size, content SHA-256 for regular files, symlink-target text plus digest for symlinks, and for every tracked path the stage-0 index mode/object ID plus index flag;
- query and selected primary target;
- explicitly editable paths;
- review-only/blast-radius paths;
- validation plan and command descriptors, but no execution result;
- prepare completeness, scan-limit, deadline, confidence, ambiguity, and coordination status;
- creation time, producer CLI version, and schema/policy identifiers.

The wire schema is exact; unknown keys and wrong/null values are invalid rather than ignored:

```text
EditBaselineV1 = {
  version: 1, schema_version: 1, edit_baseline_version: 1,
  producer_version: str, created_at: RFC3339-UTC str,
  policy: {id: "cooperative-edit-v1", sha256: lowercase-64-hex str},
  repository: {root: canonical-absolute str, identity: str,
               object_format: "sha1"|"sha256",
               git_commit: lowercase-hex str of length 40 for sha1 or 64 for sha256,
               git_ref: str|null,
               dirty: bool, dirty_tree_sha256: lowercase-64-hex str},
  request: {query: str, agent_id: nonempty str, primary_target: PrimaryTargetV1,
            editable_paths: list[repo-relative str],
            review_only_paths: list[repo-relative str],
            blast_radius_paths: list[repo-relative str],
            validations: list[ValidationDescriptorV1]},
  prepare: PrepareSnapshotV1,
  manifest: {complete: bool, path_count: nonnegative-int,
             total_hashed_bytes: nonnegative-int,
             incomplete_reasons: list[EditReason]},
  preexisting_changes: list[PathStateV1],
  trust: TrustDisclosureV1
}
ValidationDescriptorV1 = {
  id: nonempty str, argv: nonempty-list[str], cwd: repo-relative str,
  timeout_seconds: finite-float (0,3600],
  expected_exit_codes: nonempty-unique-list[int 0..255]
}
PrimaryTargetV1 = {
  file: repo-relative str, symbol: nonempty str|null,
  kind: nonempty str|null, line: positive-int|null
}
PathStateV1 = {
  path: repo-relative str, original_path: repo-relative str|null,
  porcelain_status: two-character str,
  index_mode: ("100644"|"100755"|"120000"|"160000")|null,
  index_object_id: lowercase-hex str|null whose length matches repository.object_format,
  index_flag: ("ordinary"|"assume-unchanged"|"skip-worktree")|null,
  worktree_kind: "regular"|"symlink"|"deleted"|"other",
  worktree_mode: ("100644"|"100755"|"120000")|null,
  size: nonnegative-int|null, sha256: lowercase-64-hex str|null,
  symlink_target: str|null, symlink_target_sha256: lowercase-64-hex str|null
}
TrustDisclosureV1 = {coverage: "git-visible", authorization: false,
                     ignored_paths_unobserved: true,
                     identity_trust: "self-asserted"}
PrepareSnapshotV1 = {
  result_incomplete: bool, incomplete_reasons: list[str],
  scan_limit_reached: bool, deadline_exceeded: bool,
  confidence: finite-float|null, ambiguous: bool,
  ask_user_before_editing: bool
}
EvidenceEditVerificationComponentV1 = {
  edit_verification_version: 1,
  baseline_sha256: lowercase-64-hex str,
  policy_sha256: lowercase-64-hex str,
  verifier_version: nonempty str,
  verification_result_sha256: lowercase-64-hex str,
  verdict: "PASS"|"WARN"|"BLOCK"|"INCOMPLETE",
  reasons: list[EditReason],
  coverage: "git-visible", authorization: false,
  ignored_paths_unobserved: true, identity_trust: "self-asserted"
}
```

Lists are deduplicated and canonically path/ID sorted. `review_only_paths` stores only caller-declared review scope, while `blast_radius_paths` stores the baseline builder's canonical caller-file set; they are never collapsed, so future widening can distinguish a previously observed caller from a newly observed path the caller predeclared for review. `original_path` is non-null only for a Git rename; copied files retain ordinary modified/untracked semantics because copy detection is configuration-dependent and is not a v1 promise. `index_flag=null` if and only if the path has no tracked index entry; an indexed path must carry one of the three literals. Regular files require mode/size/SHA-256 and null symlink fields; symlinks require mode/target/target digest and null byte SHA-256; deleted entries require all worktree metadata null. Strict baseline creation returns `INCOMPLETE` and writes no baseline when any `assume-unchanged` or `skip-worktree` index flag is present.

`PrepareSnapshotV1` is a pure projection over the shared prepare service's private `(legacy_payload, capsule)` pair; it does not reparse rendered text and does not change legacy `tg prepare` output. The byte-compatible prepare-service extraction is a prerequisite slice at the start of Task 6, before edit verification imports it; Task 8 extends that already-existing service rather than creating it later:

- `confidence` is `capsule.confidence.overall` only when it is a finite number, otherwise null;
- `ambiguous` is exactly `bool(capsule.ambiguity.requires_confirmation)`; `tie_requires_confirmation` is true while `none` and `tie_resolved` are false;
- `ask_user_before_editing` is exactly `bool(capsule.ask_user_before_editing.required)`;
- `scan_limit_reached` is true when any capsule `scan_limit` or `caller_scan_limit` dictionary has `possibly_truncated=true`, or `caller_scan_truncated=true`;
- `deadline_exceeded` is true when either payload has `partial=true` with `partial_reason` in `{deadline, deadline_exceeded}`, or either `deadline_limit.deadline_exceeded=true`;
- `incomplete_reasons` is the ordered subset of `scan_limit`, `deadline`, `partial_other`: add `scan_limit` for any scan signal and `deadline` for any deadline signal. Evaluate `legacy_payload` and `capsule` independently; add `partial_other` when either source has `partial=true`/`result_incomplete=true` and that same source has neither its own scan signal nor its own deadline signal. Thus a capsule scan signal cannot incorrectly explain an unrelated legacy partial (or vice versa);
- `result_incomplete` is exactly `bool(incomplete_reasons)`.

Complete, confirmation-tie, validation-resolved tie, deadline-partial, scan-truncated, unrelated-partial, and mixed scan+deadline+unrelated-source fixtures pin this projection against real prepare/capsule objects. The existing shell-shaped `validation_plan`/`validation_commands` rows are deliberately not converted into `ValidationDescriptorV1`; cross-platform shell parsing would invent policy.

The manifest is capped before reading file content (10,000 paths, 64 MiB total hashed bytes, 8 MiB per file). The canonical encoded baseline has one 5 MiB inclusive writer/reader cap; a generated 5 MiB + 1 payload is rejected before persistence, so every successfully written baseline can read and self-verify. Crossing any cap makes the baseline incomplete; no sampled subset is authorized. Status collection uses exactly `git status --porcelain=v1 -z --untracked-files=all`, so nested untracked files never collapse into an unobservable directory entry. Tracked index identity comes from NUL-delimited `git ls-files --stage -z`; index flags come from a separately bounded NUL-safe `git ls-files -v -z` adapter. Any unmerged stage 1/2/3 entry or assume-unchanged/skip-worktree flag makes the strict baseline incomplete. Verification compares current normalized worktree mode/executable metadata, content identity, and content-addressed stage-0 index metadata, so executable-bit-only mutations and an `MM` path that changes its staged content while restoring the same worktree bytes are detected. This also allows unchanged pre-existing out-of-scope dirt to remain distinct from the agent's edit delta.

The strict baseline output is confined to the tensor-grep-owned `.tensor-grep/edit-baselines/` state directory beneath the canonical repository root. No caller-selected outside-repository path is accepted. Publication is anchored to an already-opened, identity-verified owned-directory handle; path checks followed by path-based publication are forbidden. Unix creates/fsyncs the temporary through `openat(dirfd, ..., O_CREAT|O_EXCL|O_NOFOLLOW)`, publishes with no-replace `linkat(dirfd,temp,dirfd,NAME)`, cleans with `unlinkat`, and fsyncs `dirfd`. Windows opens/verifies the directory handle, creates the temp relative to it through `NtCreateFile`/`OBJECT_ATTRIBUTES.RootDirectory`, and performs a handle-relative `FileRenameInfoEx`/`FILE_RENAME_INFO` rename with `RootDirectory` and replace=false. No path-based Windows fallback is allowed; if the required handle-relative primitive is unavailable, strict readiness returns `INCOMPLETE/baseline_write_failed`. Any existing file, directory, symlink, reparse point, same-NAME race, or Event-gated parent swap returns `baseline_write_failed`; the losing `edit-ready` call removes only its temp and rolls back only its exact claim ID. That exact owned directory is excluded consistently from both baseline and verification dirty-state collection so persisting the baseline cannot invalidate itself. Unix and mandatory Windows tests swap the parent after handle verification but before temp creation/publication and prove no outside file appears.

Manifest hashing uses opened-handle verification rather than check-then-read paths. Unix opens with `O_NOFOLLOW` and verifies the opened descriptor with `fstat`. Windows opens with `CreateFileW(..., FILE_FLAG_OPEN_REPARSE_POINT, ...)`, rejects a reparse-point leaf, obtains the final path plus volume serial/file ID from that same handle, requires the final path to remain inside the canonical repository root, and hashes only through that handle. Each adapter requires a regular file, streams capped chunks up to `limit + 1`, and compares file identity/metadata before and after the read. If a platform cannot provide that no-follow/final-path opened-handle guarantee, the operation returns `INCOMPLETE` with `platform_no_follow_unavailable` before reading content; a final-path escape returns `opened_path_escape`. FIFOs/devices/other file kinds and identity changes also make the baseline incomplete. Both adapters receive deterministic Event-gated swaps. Unix has a FIFO arm; Windows CI has mandatory, non-skipped leaf-reparse and parent-junction fixtures and asserts their test IDs executed.

The baseline is invalid for a strict readiness PASS if any required field is missing, analysis is partial, ambiguity requires user input, the primary target is unresolved, the pre-existing-change manifest is incomplete, or the agent identity is anonymous.

#### `tg verify-edit`

The public Python/native argv is frozen as:

```text
tg verify-edit REPO --baseline NAME --baseline-sha256 DIGEST
  --validation-file FILE [--deadline SECONDS] --json
```

`NAME` is a basename matching `[A-Za-z0-9][A-Za-z0-9._-]{0,127}\.json`, resolved only beneath canonical `REPO/.tensor-grep/edit-baselines/`. `DIGEST` is the external trust anchor: exactly 64 lowercase hexadecimal characters. The safe reader hashes the exact opened baseline bytes and compares them to `DIGEST` before JSON/schema/policy use; a mismatch returns `INCOMPLETE/baseline_digest_mismatch`. It accepts only exact `EditBaselineV1`, never an agent capsule or heuristic conversion. `FILE` has the same exact structured-validation, confinement, duplicate, and 256 KiB contract as `edit-ready`; it supplies the current declared descriptors for drift comparison. Deadline defaults to 60 seconds and is finite in `[0.1,300]`. The command is read-only. `edit-ready` self-verification passes the SHA-256 computed from the exact no-clobber bytes it just published.

It computes:

- baseline digest validity;
- repository identity and revision drift;
- actual changed files from Git, including untracked files;
- a per-path comparison against `preexisting_changes`, so unchanged pre-existing dirt is not attributed to the edit while same-path content/status/symlink-target mutations are;
- changed paths outside editable scope;
- current target existence: re-resolve the baseline `PrimaryTargetV1.file`; when `symbol` is non-null, require the same parser-backed symbol/kind still exists in that file (line movement alone is allowed), otherwise require the file itself;
- current blast-radius floor, recomputed under the deadline from the baseline's query/structured primary target through the same prepare blast-radius builder. Paths already in `baseline.request.blast_radius_paths` are unchanged; new paths wholly inside caller-declared `review_only_paths` produce WARN; any other new path produces BLOCK;
- byte-for-byte equality between normalized descriptors in `--validation-file` and `baseline.request.validations`;
- `PASS | WARN | BLOCK | INCOMPLETE`, reasons, and a receipt digest.

`PASS` requires a complete Git-visible comparison and no scope/revision/policy violations. `WARN` is a complete comparison whose only advisory condition is blast-radius widening wholly contained within already-declared review-only paths. `BLOCK` is a complete comparison that found a Git-visible violation. `INCOMPLETE` means the comparison itself could not establish the contract.

The exact result and strict-readiness ticket schemas are:

```text
EditVerificationResultV1 = {
  version: 1, schema_version: 1, edit_verification_version: 1,
  verifier_version: nonempty str,
  verdict: "PASS"|"WARN"|"BLOCK"|"INCOMPLETE",
  reasons: list[EditReason], baseline_sha256: lowercase-64-hex str|null,
  policy_sha256: lowercase-64-hex str|null,
  repository: {root: canonical-absolute str|null, identity: str|null,
               object_format: ("sha1"|"sha256")|null,
               git_commit: format-consistent lowercase-hex str|null,
               dirty_tree_sha256: lowercase-64-hex str|null},
  changed_paths: list[PathDeltaV1], current_primary_target_exists: bool|null,
  current_blast_radius: list[repo-relative str],
  validation_descriptors_match: bool|null,
  receipt_sha256: lowercase-64-hex str|null,
  trust: TrustDisclosureV1,
  path_domain: PathDomainEvidenceV1|null
}
PathDeltaV1 = {
  path: repo-relative str,
  change_types: nonempty-list["added"|"deleted"|"renamed"|"content"|
                              "worktree_mode"|"index_mode"|"index_object"|
                              "status"|"kind"|"symlink_target"],
  before: PathStateV1|null, after: PathStateV1|null
}
EditReadyTicketV1 = {
  version: 1, schema_version: 1, edit_ready_version: 1,
  status: "ready"|"blocked"|"incomplete", reasons: list[EditReason],
  agent_id: str|null, claim_id: str|null,
  baseline_path: canonical-absolute str|null,
  baseline_sha256: lowercase-64-hex str|null,
  prepare: PrepareSnapshotV1|null,
  verification: EditVerificationResultV1|null,
  trust: TrustDisclosureV1,
  path_domain: PathDomainEvidenceV1|null
}
```

`EditReason` has this complete precedence order; results sort first by this rank and then by canonical path: `invalid_json`, `duplicate_json_key`, `unsupported_schema`, `missing_required_field`, `invalid_field`, `input_limit`, `path_domain_mismatch`, `baseline_byte_limit`, `baseline_digest_mismatch`, `result_byte_limit`, `path_limit`, `total_byte_limit`, `file_byte_limit`, `git_output_limit`, `index_unmerged`, `index_flag_unsafe`, `platform_no_follow_unavailable`, `opened_path_escape`, `unsafe_file_type`, `file_identity_changed`, `manifest_incomplete`, `repository_mismatch`, `revision_drift`, `preexisting_state_changed`, `out_of_scope_change`, `target_missing`, `validation_descriptor_drift`, `blast_radius_widened_outside_review_scope`, `blast_radius_widened_within_review_scope`, `prepare_incomplete`, `ambiguity_requires_input`, `anonymous_identity`, `claim_overlap`, `claim_fence_timeout`, `claim_fence_error`, `ledger_error`, `baseline_write_failed`, `self_verification_failed`.

The mapping is exhaustive and disjoint. `{invalid_json, duplicate_json_key, unsupported_schema, missing_required_field, invalid_field, input_limit, path_domain_mismatch, baseline_byte_limit, baseline_digest_mismatch, result_byte_limit, path_limit, total_byte_limit, file_byte_limit, git_output_limit, index_unmerged, index_flag_unsafe, platform_no_follow_unavailable, opened_path_escape, unsafe_file_type, file_identity_changed, manifest_incomplete, prepare_incomplete, ambiguity_requires_input, anonymous_identity, claim_fence_timeout, claim_fence_error, ledger_error, baseline_write_failed, self_verification_failed}` maps to verification `INCOMPLETE`, ticket `incomplete`, and exit 2. `{repository_mismatch, revision_drift, preexisting_state_changed, out_of_scope_change, target_missing, validation_descriptor_drift, blast_radius_widened_outside_review_scope, claim_overlap}` maps to verification `BLOCK`, ticket `blocked`, and exit 1. `blast_radius_widened_within_review_scope` maps to verification `WARN`, ticket `blocked` (strict readiness requires PASS), and exit 1. Empty reasons map to verification `PASS`, ticket `ready`, and exit 0. WSL translation failure uses the same full key set, `path_domain.status="error"`, reasons exactly `['path_domain_mismatch']`, all unavailable repository/change/claim/baseline/prepare/verification fields null or empty per type, and exit 2/no state. Successful WSL calls carry translated evidence; non-WSL calls carry `path_domain=null`. Tests enumerate every enum member and fail if it appears in zero or multiple sets.

`EditVerificationResultV1.receipt_sha256` reuses `evidence_signing.receipt_digest` and `canonical_receipt_bytes` (`tg-canonical-json-v1`): compact key-sorted ASCII JSON with top-level `receipt_sha256` and `signature` excluded; this result has no signature. Digest-stability and one-field mutation tests pin the preimage rule. The result uses one canonical compact UTF-8 serializer with a 5 MiB inclusive final-wire cap; CLI measurement includes its trailing newline, and evidence ingestion measures the exact bytes read from its file or stdin transport. If a complete result would exceed the cap, the producer emits the same full key set with `verdict="INCOMPLETE"`, ordered reasons prefixed by `result_byte_limit`, `changed_paths=[]`, `current_blast_radius=[]`, `validation_descriptors_match=null`, and `receipt_sha256` recomputed for that bounded envelope. It never emits a sampled partial as PASS/BLOCK/WARN. Fixed-size fields plus bounded paths guarantee the fallback fits, asserted at cap−1/cap/cap+1; therefore every emitted result can be ingested by `tg evidence emit`.

The threat model is cooperative-agent safety, not a hostile local-user sandbox. Git ignored files outside declared editable paths are not observable from Git status, and a keyless receipt digest proves only self-consistency/deduplication. Every payload therefore carries `coverage: "git-visible"`, `authorization: false`, `ignored_paths_unobserved: true`, and `identity_trust: "self-asserted"`. Documentation and consumers must not call a PASS an authorization decision. Declared editable ignored files are explicitly hashed within the same caps, but detecting arbitrary writes to other ignored paths requires an external filesystem sandbox and is outside this command.

Every file-backed baseline, validation descriptor set, verification result, primary receipt, and previous receipt uses one safe bounded JSON reader. It confines the supplied path to its declared repository/owned root; opens a regular file through the same Unix `O_NOFOLLOW` or Windows reparse-rejecting/final-path handle adapter used by manifest hashing; reads at most `cap+1` through that handle; verifies final path plus file identity before/after; and only then decodes through a duplicate-rejecting `object_pairs_hook`. FIFOs/devices, leaf links/reparse points, parent junction escapes, identity swaps, oversize files, and duplicate keys at any nesting depth fail before semantic use. Caps are transport-specific (5 MiB baseline/verification/receipt, 256 KiB validation file) but the safety adapter is singular. The evidence-only `--edit-verification -` sentinel instead reads standard input exactly once to EOF or `cap+1`, applies the same exact-byte cap and duplicate-rejecting decoder, and cannot be combined with any other stdin-consuming evidence option. Verification never relies on Python's last-key-wins behavior. The same semantic component validation runs after cryptographic verification for signed and keyless receipts: when `edit_verification` is present, all four trust fields are mandatory and exact even on a freshly and correctly re-signed receipt. Legacy receipts with no component remain valid.

The component has a production ingress through the additive existing-subcommand option:

```text
tg evidence emit REPO --edit-verification FILE|- [existing evidence/signing options] --json
```

Evidence emit translates its complete typed set before any read/sign/write. On a successful WSL call, the exact `PathDomainEvidenceV1` object is an optional top-level receipt component included in canonical digest/signature bytes; non-WSL and legacy receipts omit it and remain byte-compatible. On bridge failure, no receipt is built or written: the existing full evidence error-envelope key set gains `path_domain`, `result_incomplete=true`, and `incomplete_reason_class="path_domain_mismatch"`, with exact `error={"code":"path_domain_mismatch","message":"path-domain translation failed","reason":PathDomainReason}`, then exits 2. Signed/keyless, canonical-alias, and no-read/no-sign/no-write tests pin both paths.

`FILE` must resolve inside `REPO` through the safe bounded-reader adapter; `-` is the production verify-to-evidence handoff and consumes the exact `verify-edit --json` stdout bytes from standard input without materializing a repository file that would change the dirty digest. Both transports are capped by the same 5 MiB verification-result limit before decoding as exact `EditVerificationResultV1`. The builder first verifies the result's own `receipt_sha256` against its exact canonical bytes. It copies the result's digest to `verification_result_sha256` and copies the result-producing `verifier_version` verbatim; the current evidence emitter never relabels an older result as newly verified. The receipt builder—not the adapter—captures the canonical root, repository identity, object format, commit, and dirty-tree digest exactly once through the same revision helper/exclusion policy used by `verify-edit`. Inside that same builder call it compares the result to this immutable capture, derives `EvidenceEditVerificationComponentV1`, places the identical captured subject in the outer receipt, and signs it; there is no second Git read and no caller-supplied revision override. Cross-repository inputs and clean/dirty/revision drift visible at that capture exit 2. An Event-gated mutation between adapter read and builder capture must therefore mismatch and fail; a mutation after capture does not rewrite the signed subject and is honestly represented as occurring after the captured revision. Malformed, duplicate-key, null/invalid result digest, or semantically inconsistent input also exits 2 without emitting a receipt. Existing invocations without the option and legacy receipts without the component remain byte-compatible. The option may coexist with the existing capsule/manifest inputs provided none consumes stdin and is exercised through Python and compiled-native front doors in both keyless and signed modes. A subprocess-based round trip runs `verify-edit`, checks its exit code directly, passes its captured stdout bytes as evidence stdin without a shell pipeline, and checks the signed/keyless evidence exit and subject. It proves PASS producer 0/consumer 0; valid WARN and BLOCK producer 1/consumer 0 with the producer status retained and receipt verdict unchanged; digest-valid `result_byte_limit` INCOMPLETE producer 2/consumer 0 with the failure retained and receipt verdict unchanged; and malformed/null/invalid-digest input consumer 2 with no receipt. Direct shell piping is unsupported unless the invoking shell/harness preserves and checks both process statuses; a consumer zero can never erase producer 1/2.

Exit policy:

- 0: `PASS`;
- 1: `WARN` or `BLOCK` with a complete result;
- 2: `INCOMPLETE`, invalid input/schema, or failed verification.

Validation execution is not part of this program because it creates a command-execution trust boundary absent from cooperative comparison. It remains owned by the security program with trigger “a separately approved allowlist/execution policy and threat model”; only then may a dedicated PR produce command-result evidence.

#### `tg edit-ready`

This is a strict cooperative-readiness composition over shared prepare and baseline builders. It never changes legacy `prepare` or ledger semantics and never claims to be a security authorization boundary.

The public Python/native argv is frozen as:

```text
tg edit-ready REPO QUERY [--agent-id ID] --validation-file FILE --out NAME
  [--editable PATH ...] [--review-only PATH ...] [--deadline SECONDS] --json
```

`QUERY` is 1..16,384 UTF-8 bytes. `--agent-id` is parser-optional so omission reaches the full JSON error envelope, but the service requires it for readiness; when present, `ID` matches `[A-Za-z0-9][A-Za-z0-9._-]{0,127}`. There is no configured-identity fallback on this strict surface. `--deadline` defaults to 60 seconds and is finite in `[0.1,300]`; strict readiness has no unbounded arm. `NAME` uses the same basename grammar as `verify-edit` and always resolves beneath `.tensor-grep/edit-baselines/`. `FILE` is read by the shared safe JSON adapter at cap−1/cap/cap+1 around 256 KiB and contains an exact array of 1..32 descriptors; zero/33 rows, unknown/duplicate/null keys, and wrong JSON types are invalid. Descriptor IDs are unique and match the agent-ID grammar. `cwd` must normalize to an existing canonical directory inside the repo. `argv` has 1..64 strict JSON strings: `argv[0]` is nonempty, all strings reject NUL, each is at most 8,192 UTF-8 bytes, and the total is at most 65,536 bytes. `timeout_seconds` is a finite JSON number but not a boolean; `expected_exit_codes` has 1..32 unique strict JSON integers (booleans excluded) in 0..255. Duplicate normalized `(cwd, argv)` descriptors are invalid. The existing shell-shaped prepare validation rows are advisory only and never synthesized into argv.

Scope normalization is singular and deterministic. The structured prepare primary target is always editable. Caller `--editable` paths are optional additions (maximum 256 total). Caller `--review-only` paths normalize into `request.review_only_paths`; every `blast_radius_floor.top_callers[].file` normalizes separately into `request.blast_radius_paths`. The three final lists are disjoint with precedence `editable > blast_radius > review_only`: derived blast identities already editable are removed; caller review identities also present in derived blast are stored only in blast. A caller explicitly repeating the primary/editable identity, repeating an option identity, or placing one identity in both caller categories is invalid rather than silently deduplicated; duplicate derived blast rows are safely deduplicated. Path identity uses canonical existing handles/parents plus `normcase` on case-insensitive platforms, so spelling/case aliases cannot form two scopes; output retains one canonical repo-relative slash spelling and is identity-deduplicated then lexical-sorted. NULs, absolute/out-of-root paths, symlink/junction escapes, or more than 256 total identities across all three scope lists are `invalid_field`/exit 2. Existing editable/review-only/blast leaves must be regular files inside the repo; a not-yet-created editable leaf is allowed only when its nearest existing parent is a real directory inside the repo without a symlink/reparse escape, and two nonexistent leaves that normalize to one platform identity are duplicates. Nonexistent review/blast paths, directories as edit leaves, and special/reparse/symlink leaves are invalid. The exact normalized structured primary target and three separate scope lists—not caller ordering—are stored in `EditBaselineV1.request`.

Exit 0 requires all of:

- explicit valid `--agent-id` (no configured-identity fallback);
- a resolved primary target;
- no `ask_user_before_editing` requirement;
- complete capsule and blast-radius analysis;
- an atomic `--out` baseline write;
- a successfully submitted claim owned by that named identity;
- zero pre-existing overlapping claims across all identities, including the same self-asserted agent ID;
- at least one structurally valid validation descriptor;
- a self-verification pass over the persisted baseline.

Every claims-index mutation path—legacy submit, strict submit, and release—uses a callback-style claims RMW API that acquires one crash-released per-root OS fence, reads under it, invokes the mutation callback, and releases only after any required publication. The sole pre-fence exception is release's existing absent-index fast path: a missing claims index returns its legacy no-op immediately and creates neither ledger directories nor a fence; because no claims record exists to remove, this is not an RMW mutation. A callback returns either `WRITE(records, result)` or `NO_WRITE(result)`; only `WRITE` atomically publishes. This preserves an existing-index/no-match release byte-for-byte, including inode and mtime. The callback never receives a publish handle or writable snapshot it can commit later. The fence is a claims-only, stable `<claims-index>.fence` artifact that normal operation never unlinks or atomically replaces; it is distinct from the stale-reclaimable lease file and does not silently alter checkpoint/session/finding lock semantics. Starting from an identity-verified canonical-root handle, missing claims-state directory components are created only handle-relative (`mkdirat`-style on Unix, `NtCreateFile(...RootDirectory...)` on Windows); unsupported relative creation fails closed. The implementation holds the final verified state-directory handle, creates/opens the fence relative to it, and reads/publishes the claims index only relative to that same handle. Unix uses `openat(state_dirfd, ..., O_RDWR|O_CREAT|O_CLOEXEC|O_NOFOLLOW, 0600)`, then `flock(fence_fd, LOCK_EX|LOCK_NB)`; Windows uses `NtCreateFile` with the state-directory handle as `RootDirectory`, read/write sharing but no delete sharing, then `LockFileEx(LOCKFILE_EXCLUSIVE_LOCK|LOCKFILE_FAIL_IMMEDIATELY)` over byte `[0,1)`. The state-directory and fence handles plus exclusive lock remain held across index read, callback, handle-relative publication/fsync, or `NO_WRITE`, and release in `finally`/RAII only after the transaction ends; process death releases the OS lock. A path-based `mkdir`, `O_CREAT`, `CreateFileW(OPEN_ALWAYS)`, read, or rename after a parent check is forbidden. Revalidate canonical-root/state-directory identity before fence creation, after lock acquisition before index read, and immediately before publication. Both platforms poll nonblocking acquisition every 20 ms for at most 12 s in production and use a sub-second injected deadline in tests. A timeout raises `ClaimsFenceTimeoutError`, a subclass of the already-mapped `IndexLockTimeoutError`, so legacy commands keep their existing exit-2 behavior; strict JSON maps it to `claim_fence_timeout`/exit 2. The acquisition order is always OS fence before any retained lease metadata. The reclaimable lease may remain diagnostic, but it is never the serialization or fencing primitive. Event-gated two-process tests swap an intermediate state-directory component before anchored directory/fence creation, after lock acquisition, and before claims-index publication; every arm must fail closed with no split-brain success, no claim mutation, and a byte-identical external tree. The strict operation checks every pre-existing overlap and inserts only while holding the OS fence; identity never suppresses an overlap. Exactly one of two concurrent conflicting strict claims may succeed. Same-root operations mutually exclude while different roots remain independent. Any ledger read/fence/write failure is fail-closed on this strict surface even though legacy `prepare` remains advisory. A conflicting strict call emits a full ticket with `status="blocked"`, `reasons=["claim_overlap"]`, null claim/baseline fields, exit 1, and no state write. Every ticket carries `authorization: false`; its named identity is coordination metadata, not authenticated ownership.

### Program 3 — foundational-language caller/reference depth

Five sequential releases: Java, C#, PHP, C, C++. Each implements grammar-backed reference/call extraction through the existing `LanguageDescriptor.references_and_calls` seam. C and C++ may share design utilities but remain separately tested and released.

Per-language acceptance:

- live grammar/node-shape fixture;
- free function/static call;
- instance/member call;
- constructor/type reference where the grammar supports it;
- same-name definition and string/comment decoys;
- nested/qualified name behavior;
- grammar unavailable and parse-error honesty;
- deterministic ordering and deduplication;
- pinned agent/retrieval ranking output;
- no fabricated cross-file target.

Cross-file import resolution is a follow-on architecture slice for Java, Go, PHP, C#, C, and C++. It reads real package/build/module/include configuration and reverse-confirms that the target exports the symbol. Every configuration reader shares one bounded no-follow contract: at most 64 files, 8 MiB per file, 32 MiB total opened bytes, 10,000 literal mapping/include rows, and 10,000 derived roots. It opens and hashes through identity-verified handles confined to the workspace, refuses absolute or `..` mappings outside the workspace plus symlink/junction escapes, and never reads a rejected target. Malformed/oversized/dynamic/out-of-root inputs produce explicit fixed-vocabulary resolution gaps without a partial mapping. Unix and mandatory Windows Event-gated tests swap a config leaf and parent before/during read and prove no external bytes are consumed. Unresolved mappings remain explicit.

### Program 4 — federated multi-root prepare

Refactor the existing prepare composition into a reusable service, then add a versioned workspace aggregator.

Contract:

- explicit roots only; no implicit parent-directory crawl;
- canonical, non-overlapping roots confined to the declared workspace anchor;
- maximum eight roots;
- one shared absolute deadline;
- deterministic root ordering and stable result ordering;
- per-root result, error, partial reason, and omitted-root record;
- aggregate `result_incomplete=true` when any required root is omitted/incomplete;
- no “missing result means no conflict” interpretation;
- token/output ceilings and 1/2/8-root CI benchmarks;
- ledger claims remain root-scoped until CEO-gated cross-root enforcement is designed.

The CLI is a separately versioned command:

```text
tg workspace-prepare ANCHOR QUERY --root ROOT [--root ROOT ...] [--deadline SECONDS] --json
```

`--root` is semantically required and repeatable from one through eight values; the Typer adapter accepts an empty list so zero-root input reaches the service and produces the JSON error envelope rather than parser prose. Relative roots resolve against `ANCHOR`; absolute roots are accepted only when their canonical path is contained by canonical `ANCHOR`. Symlink escapes, duplicate canonical roots, nested roots, zero roots, and more than eight roots are input errors. Query input is 1..16,384 UTF-8 bytes; each supplied/canonical anchor or root is at most 32,768 UTF-8 bytes; deadline must be finite with `0 < seconds <= 300`. Even one root returns the workspace schema rather than the legacy single-root `prepare` payload, leaving `tg prepare` byte-compatible. Roots execute sequentially in canonical path order under one shared absolute deadline; the campaign makes no root-parallelism claim.

The shared service and CLI return exactly one typed workspace schema version 1 envelope: `{version:1, schema_version:1, workspace_prepare_version:1, routing_backend:"prepare-service-v1", routing_reason:"explicit-roots"|"invalid-input"|"path-domain-error", anchor:str|null, query:str|null, roots:list[WorkspaceRootV1], root_count:int, completed_root_count:int, omitted_roots:list[OmittedRootV1], result_incomplete:bool, incomplete_reasons:list[WorkspaceIncompleteReason], error:WorkspaceErrorV1|null, path_domain:PathDomainEvidenceV1|null}`. `WorkspaceRootV1` is exactly `{root:str, status:"complete"|"partial"|"error", payload:dict[str,JSONValue]|null, error:WorkspaceErrorV1|null, incomplete_reason:null|"deadline"|"prepare_incomplete"|"prepare_error"|"output_limit"}`: complete requires non-null payload and all other nullable fields null; partial requires payload plus one of `deadline|prepare_incomplete`; error requires null payload, non-null error, and `prepare_error`; output-limit rebuilding permits null payload with `output_limit` while preserving original status. `WorkspaceErrorV1` is exactly `{code:"invalid_input"|"prepare_error"|"path_domain_mismatch", message:str}` with a nonempty sanitized message capped at 1,024 UTF-8 bytes. `OmittedRootV1` is exactly `{root:str, reason:"deadline"}`. `WorkspaceIncompleteReason` is the ordered subset `invalid_input,path_domain_mismatch,deadline,prepare_incomplete,prepare_error,output_limit`. `roots` and `omitted_roots` are mutually disjoint and each canonically path-sorted; `root_count == len(roots)+len(omitted_roots)` after valid canonicalization, `completed_root_count == count(status=="complete")`, and `result_incomplete` is true iff top-level error is non-null, an omitted/noncomplete root exists, or output limiting occurred. Success and partial results use top-level `error=null`. Non-WSL calls carry `path_domain=null`; successful WSL calls carry translated evidence. Invalid input returns the same full key set with canonical `anchor` or null, original valid-string `query` or null, empty roots/omissions, zero counts, `routing_reason="invalid-input"`, `result_incomplete=true`, reasons `['invalid_input']`, and bounded invalid-input error. Path-domain failure occurs before canonicalization/dispatch and returns the same full key set with `anchor=null`, valid query or null, empty roots/omissions, zero counts, `routing_reason="path-domain-error"`, `result_incomplete=true`, reasons `['path_domain_mismatch']`, sanitized path-domain error, and error evidence, then exits 2. One compact UTF-8 serializer owns the 8,388,608-byte inclusive final-wire cap, including `path_domain`, MCP version, and CLI newline. Output-limit rebuilding preserves path-domain/error identity. Cap−1/cap/cap+1 tests measure actual returned bytes. Exit 0 means every root completed; every invalid/schema/path-domain/path input or partial/omitted/failed root exits 2; exit 1 is unused. For fixtures below the cap, service, Python CLI, compiled native CLI, and MCP stdio are value-identical after removing only MCP transport version; success, partial, invalid, and path-domain arms enforce equality.

With absent WSL provenance, the MCP adapter retains direct canonical confinement. With WSL provenance, it grammar/size-checks caller strings, translates anchor/roots, then confines the translated anchor through `_confine_mcp_path` under `_mcp_root()` and every canonical translated root under both that resolved anchor and `_mcp_root()`. A raw WSL spelling is never passed to Windows confinement, and a caller cannot widen authority with a broader or mistranslated anchor.

CLI exposure precedes MCP exposure. MCP exposure is a separate contract-versioned PR.

### Program 5 — graph-native context without a graph runtime

Program 5 attempts two independent, read-only, rebuildable contracts after the language relation registry and federated MCP work are stable. Before either candidate branch exists, a separate non-releasing tracker-and-oracle PR lands `CODE-GRAPH-PROJECTION` and `CHANGE-IMPACT` as `READY`, plus `GRAPH-TRACE`, `GRAPH-CROSS-REPO`, `GRAPH-WORKFLOW-RUNTIME`, `GRAPH-PDG`, and `GRAPH-MUTATION` as `DEMAND_GATED`; it also lands only the frozen schemas, hand-authored gold fixtures, and independent validators needed to judge both candidates, never production graph behavior. A candidate PR carries its own `IN_FLIGHT` transition. Each build row must pass its own correctness, safety, and incremental-value gate before merge. A failed value gate closes that implementation PR unmerged and lands a non-releasing retirement PR that records the closed PR and evidence and moves the canonical main-branch row directly from `READY` to `RETIRED`; this is a narrowly tested exception for exactly these two value-gated IDs because the candidate's `IN_FLIGHT` commit intentionally never merged. It is never published and then relabeled `RETIRED`. A successful candidate follows the ordinary merged `READY` → `IN_FLIGHT` → separately verified `SHIPPED` lifecycle.

`CodeGraphProjectionV1` is an additive `tg map PATH --format graph-v1 --json` representation. With no `--format`, existing `tg map` bytes/exits and its legacy cap/deadline flags remain pinned. Graph-v1 has a closed stricter argv branch: `--json` is required; omitted `--deadline` means 60 seconds; a syntactically parsed explicit deadline is finite and in `[0.1,300]`; `--no-deadline`, any explicit `--max-files`, and any explicit `--max-repo-files` are invalid-input/exit 2. Lexically nonnumeric deadline text and the inherited Click `min=0.1` rejection occur before command dispatch and retain the already-pinned legacy Click exit-2/parser-stderr contract; they cannot truthfully emit a graph envelope. Every post-parse graph-format incompatibility, including nonfinite or above-300 values, returns the full invalid-input envelope. The adapter uses the Click/Typer parameter-source API to distinguish the legacy `--max-repo-files=512` parser default from an explicit occurrence, then installs the graph contract's fixed 5,000-file cap. No legacy flag can weaken or silently narrow the reported graph limits. Accepted requests report their effective deadline; a post-parse deadline/cap error uses the same full envelope with `limits.deadline_seconds=60` and every other frozen limit unchanged. Its exact envelope is `{version:1, schema_version:1, graph_projection_version:1, artifact:"code_graph_projection", routing_backend:"repo-map-graph-v1", repository:GraphRepositoryV1|null, revision:GraphRevisionV1|null, nodes:list[GraphNodeV1], edges:list[GraphEdgeV1], coverage:GraphCoverageV1, resolution_gaps:list[GraphGapV1], omitted:GraphOmissionsV1, result_incomplete:bool, incomplete_reasons:list[GraphIncompleteReason], limits:GraphLimitsV1, path_domain:PathDomainEvidenceV1|null, error:GraphErrorV1|null}`.

The projection requires a confined Git worktree with a resolved `HEAD`. `GraphRepositoryV1` is `{root:".", identity_sha256:LowerHex64, identity_authoritative:false}`. The identity preimage is the UTF-8 compact JSON array `["tensor-grep-repository-v1", object_format, sorted_root_commit_oids_reachable_from_HEAD]`, with every string NFC-normalized; it is stable across clones and moved roots of the same reachable history, changes when history lineage changes, includes neither path nor remote, and is not authorization. Every explicit impact `base`/`head` commit must have exactly the same sorted root-commit set as current `HEAD`; an orphan or unrelated-history ref is `invalid_ref`, lower-bound, and exit 2 rather than producing graph IDs that cannot join the projection. `GraphRevisionV1` is `{object_format:"sha1"|"sha256", commit_sha:LowerHex40|LowerHex64, dirty_tree_sha256:LowerHex64|null, dirty:bool, capture_consistency:"stable_non_atomic", snapshot_atomic:false}`, with dirty digest non-null iff dirty. Stable reads are request-consistent evidence, not an operating-system filesystem snapshot; cross-file atomicity is never claimed.

There is exactly one repository node. Every included path appears exactly once as either `file` or `test`; test-classified paths never also appear as files. Repository node fields other than ID/type/provenance are null. File/test nodes require only normalized path plus fixed type/provenance; their name/kind/language/line fields are null. Symbol nodes require path, `name` equal to the NFC canonical qualified name used for grouping/ID hashing, symbol kind, language, one-based start/end lines, and parser provenance, and their path names an included file/test node. Every file/test has exactly one inbound repository `contains` edge; every symbol has exactly one inbound `defines` edge. The endpoint matrix is fixed: `contains` is repository→file/test with `repository/exact`; `defines` is file/test→symbol with `parser/exact`; `imports` is file/test→file/test with `resolver/exact`; `validates` is test→file/symbol with `test-matcher/heuristic`. Self-edges, dangling endpoints, reversed relations, duplicate complete records, normalization collisions, and any other provenance/confidence combination are invalid and make the projection incomplete.

All IDs are lowercase SHA-256 over UTF-8 compact JSON arrays with NFC strings and no insignificant whitespace. Repository ID hashes `["tg-graph-node-v1", repository_identity, "repository"]`; file/test IDs hash the same domain/repository/type/path; symbol IDs hash domain/repository/`symbol`/path/language/kind/qualified name/duplicate ordinal. The ordinal is zero-based after sorting same-key definitions by `(start_line,end_line,qualified_name,kind,language)`, so unrelated line insertion does not change the ID when relative duplicate order is unchanged. Edge IDs hash `["tg-graph-edge-v1", repository_identity, relation, source_id, target_id, provenance, confidence]`. The repository identity scopes equal-looking nodes from different histories.

V1 materializes only `contains` and `defines` from one pure captured map. `imports` and `validates` remain `query_only|unsupported` until an already-materialized resolver receipt can be consumed without rescanning; calls/references are not edge types and are disclosed only in coverage as `query_only|unsupported`. `GraphCoverageV1` always records `path_scope:"repository_local_tracked_plus_nonignored_untracked"`: every supported stage-0 tracked path remains in scope even if an ignore pattern matches, while ignore rules select only untracked paths. It also records the fixed policy `git_admin_source_channel:"excluded"`; `ignored_content_unread:true`; `external_git_config_unobserved:true`; `attributes_transforms_unapplied:true`; `capture_consistency:"stable_non_atomic"`; `snapshot_atomic:false`; and actual phase booleans `worktree_census_started`, `path_names_observed`, `local_ignore_evaluation_started`, `local_ignore_evaluation_completed`, and `worktree_untracked_evaluation_completed`. All phases start false. Census flips immediately before the first directory enumeration attempt; path observation flips after the first entry name is returned; ignore-start flips immediately before root untracked-selection policy evaluation begins, including an empty candidate set; ignore-completed flips only after the whole hierarchical selection finishes; untracked-completed flips only after its selected set is stably captured, including successful capture of an empty set. Error/final-wire arms preserve actual reached phases, so the disclosure never depends on nullable revision metadata. Empty edges never mean repository absence outside that scope. Fixed gaps and per-language coverage disclose grammar, parse, dynamic, config, mapping, unsafe-path, resource, deadline, and output limitations.

The projection uses an anchored component-by-component beneath-root/no-link/no-mount traversal; it never follows a nested symlink, junction, bind mount, volume/mount/reparse escape, or parent swap. `PATH` must itself be the confined Git top-level—no silent ascent from an MCP-authorized subdirectory. One shared absolute deadline and pre-analysis caps bound 256 root commits, 20,000 visited filesystem/history/object entries, 5,000 parsed files, 32,768 UTF-8 bytes per repository path, 64 ignore files/10,000 ignore rules/2 MiB aggregate ignore bytes/10,000,000 ignore-matcher transitions, 2 MiB/100,000 lines per opened source or Git object, 32 MiB logical and 64 MiB physical double-captured source/worktree/ignore bytes, 64 MiB physical object-entry reads and 128 MiB cumulative decompressed object/delta/diff intermediates, 64 MiB logical/128 MiB physical metadata capture, 256 MiB per consumed pack/512 MiB aggregate streamed pack verification, 64 MiB live buffers, 2,000,000 parser nodes, 5,000 output nodes, 4,000 edges, 10,000 gaps, and 8,388,608 final transport bytes. Re-reading or re-expanding an object/delta charges again; released memory never refunds cumulative bytes. Ignore matching is a non-backtracking token/path-character state machine; every transition charges the shared operation ledger and checks the absolute deadline inside the matching loop, so one adversarial wildcard cannot monopolize a call. The 4,000-edge cap is deliberately below the node cap, so a valid contains/defines-only graph can cross it first. Cap+1 stops traversal/parse, rather than truncating only after materialization. Both graph actions make zero filesystem changes to repository files, Git state, tensor-grep persistent caches/indexes/sessions/ledger, temporary storage, or caller-visible state; graph-v1 uses a pure no-persist builder mode even if a legacy path is later found to write. They launch no Git child and create no scratch directory.

Projection truncation retains the repository node, then sorted file/test nodes, then symbols whose parent remains; it retains an edge only when both endpoints remain. Each omission field is `OmissionCountV1={count:int|null, exact:bool}`: `exact=true` iff `count` is a nonnegative exact count, while an early traversal/parser/deadline cap that makes the unseen population unknowable uses `{count:null,exact:false}`. Projection pre-identity errors have `repository=null` and `nodes=[]`. Once projection identity exists, every projection analysis/error/output arm retains exactly one repository node; final-wire rebuilding clears every non-repository node and all edges, adds the exactly known cleared population only when the prior omission state was exact, otherwise preserves unknown, retains identity/coverage/limits/path-domain/error, and emits an `output_limit` gap. No retained edge/reference may target omitted data. Complete exit is 0; every input/error/partial result is 2; exit 1 is unused. Fixed error-code templates forbid raw parser details, exception repr, refs, absolute paths, usernames, remotes, environment values, and source text.

`ChangeImpactV1` is `tg change-impact PATH --base REF [--head REF] --json`; explicit head is one immutable commit transition and never reads worktree content. Omitted head is an exact ordered three-snapshot comparison: `staged` compares base tree→captured stage-0 index, `unstaged` compares that captured index→captured worktree for tracked paths, and `untracked` compares absence→bounded non-ignored untracked regular files. The output contains a separate event per nonempty transition, so an A→B staged change followed by B→A worktree restoration remains two visible events instead of a false-clear net zero. In explicit-head mode `revision.commit_sha` is that resolved head with `dirty=false` and null dirty digest; omitted-head mode reports current HEAD plus a digest binding the same captured index, worktree, and untracked manifests used by the events. REF is exactly `HEAD`, a lowercase full-format 40/64-hex OID matching the repository object format, or a fully qualified ASCII `refs/heads/...` / `refs/tags/...` name under the frozen 1,024-byte component grammar; abbreviated OIDs, short names, reflog/date/path syntax, `~`, `^`, `:`, option-looking values, other namespaces, invalid `.lock`/dot/slash/control/backslash forms, and ambiguous branch/tag shorthand are refused. Symbolic refs and annotated tags use at most eight hops with cycle detection. Omitted/null deadline means 60 seconds; explicit deadline is finite non-boolean `[0.1,300]`; pre-validation deadline errors report the full envelope with the default 60-second limit. Its exact envelope is `{version:1, schema_version:1, change_impact_version:1, artifact:"change_impact", routing_backend:"git-diff-impact-v1", repository:GraphRepositoryV1|null, revision:GraphRevisionV1|null, diff:DiffIdentityV1|null, changed_files:list[ChangedFileV1], changed_symbols:list[ChangedSymbolV1], affected_files:list[AffectedFileV1], affected_tests:list[AffectedTestV1], validation_hints:list[ValidationHintV1], resolution_gaps:list[GraphGapV1], trust:ImpactTrustV1, omitted:ImpactOmissionsV1, result_incomplete:bool, incomplete_reasons:list[ImpactIncompleteReason], limits:ImpactLimitsV1, path_domain:PathDomainEvidenceV1|null, error:GraphErrorV1|null}`. Validation hints are bounded opaque display strings copied without parsing, explicitly `authorization:false` and `trust:"untrusted_inert"`; tensor-grep and the interoperability cookbook never auto-execute them.

Each `ChangedFileV1` is an identity-bearing transition event with `phase:"commit"|"staged"|"unstaged"|"untracked"`, normalized before/after modes (`100644|100755|120000|160000|null`), regular/symlink content digests, opaque submodule gitlink OIDs, hunks, and status. Explicit-head mode permits only `commit`; omitted-head mode permits only `staged`, `unstaged`, and `untracked`. Its canonical ordered `change_types` subset is drawn from `added,deleted,renamed,content_modified,mode_modified,type_modified,untracked`. Executable-bit-only changes and regular-file/symlink/submodule transitions are visible even when bytes match; submodule contents are never traversed, but their before/after gitlink identities remain exact. Two records may share a path only across distinct phases; the event ID binds phase, paths, modes, digests/gitlink OIDs, change types, repository/base/head identity. `ChangedSymbolV1` binds its parent file-event ID and phase, nullable before/after node IDs and spans, and a canonical nonempty ordered subset of `added,deleted,renamed,content_modified`; rename plus edit is representable, added/deleted side nullability is exact, and ambiguous pairing is incomplete. Affected evidence uses typed sources `{kind:"changed_file", changed_file_id:str, symbol_event_id:null}` or `{kind:"changed_symbol", changed_file_id:null, symbol_event_id:str}`. Every retained source resolves to a retained transition event. Same-target records union across phases by kind/path/reason/provenance; `heuristic` conservatively dominates `exact`, and canonical event sources are unioned. Same display/reason validation hints likewise union sources. Impact caps and omissions count phase-event records, not unique paths. Truncation retains its non-null `repository` metadata plus phase-sorted changed-file events, then only symbols whose parent file event remains; affected records/hints filter to retained sources and drop only if no source remains. Any event/source truncation forces lower-bound trust. Final-wire rebuilding clears impact evidence lists under the same exact-or-unknown omission rule and forces `ImpactTrustV1` to lower-bound: every previously applicable relation becomes lower-bound, `lower_bound_relations` lists those relations in declaration order, and the statement becomes `lower_bound_due_to_gaps`.

`ImpactTrustV1` separates requested scope from executed evidence. Type-valid service/CLI/MCP omitted or null head is `requested_head_kind:"worktree"` with `repository_local_base_index_worktree_plus_nonignored_untracked` coverage; a non-null string is `commit` with commit-diff coverage. Schema/type violations never construct an impact envelope: CLI retains its parser error, core service programmer misuse raises the pinned typed exception, and TG's low-level direct/stdio MCP boundary returns protocol `-32602 invalid graph arguments` with no data or tool result. Actual worktree phases match projection: `worktree_census_started` flips immediately before the first enumeration attempt; `path_names_observed` on the first returned entry; `local_ignore_evaluation_started` immediately before root untracked-selection policy evaluation begins, including an empty candidate set; `local_ignore_evaluation_completed` only after hierarchical selection finishes; and `worktree_untracked_evaluation_completed` only after stable capture of the selected untracked set, including an empty set. All start false, remain false in commit mode, and errors preserve precisely reached phases. Trust also carries the fixed policy `git_admin_source_channel:"excluded"`; `ignored_content_unread:true`; `external_git_config_unobserved:true`; `attributes_transforms_unapplied:true`; `capture_consistency:"stable_non_atomic"`; `snapshot_atomic:false`; `authorization:false`; `validation_hints_trust:"untrusted_inert"`; fixed relation states/lower-bound list/statement. Commit comparison uses verified objects only. Omitted-head comparison uses the captured base/index/worktree providers and local ignore sources, never porcelain/global/attribute semantics. Names—including ignored names—may be observed for safe decisions but ignored content and the administrative directory's worktree/source channel remain unopened. Empty diff is complete only within head-specific coverage; non-exhaustive or unsafe/unsupported/truncated evidence is lower-bound/incomplete/exit 2.

Commit-head enrichment is object-backed end to end. The reader exposes bounded immutable base/head `path→(mode,verified bytes)` providers. Base and head both drive changed-file/symbol attribution, span intersection, and rename pairing. Every affected caller, importer, test, resolver/config decision, and validation hint is evaluated against the transition's after-provider only: callers/tests removed by the change are not current affected evidence; newly added after-side evidence is; a deleted symbol's before identity may seed a lookup but all retained relation targets come from that phase's after-provider. Parser/resolver/test/policy adapters never open live paths. A missing object-backed relation adapter omits that relation, marks it lower-bound, and emits its fixed gap; hints come only from verified after-provider policy or are omitted under the same rule. Arbitrary current worktree/untracked/ignore/policy mutations leave explicit-head bytes identical and a worktree-content canary untouched. Omitted-head mode freezes three providers once—base, stage-0 index, and captured worktree—and runs the same policy independently over base→index (`staged`), index→worktree (`unstaged`), and absence→worktree (`untracked`) events without rescanning. Affected targets are unioned by target identity; their canonical source lists preserve every phase event that reached them. An index-only caller/test may support staged evidence, a worktree-only caller/test may support unstaged/untracked evidence, and neither provider may substitute for the other.

One in-process `BoundedGitReaderV1` replaces every Git subprocess and scratch snapshot. Starting from an authorized root handle, it resolves only an in-root `.git` directory; V1 deliberately refuses `.git` file indirection, external/absolute/`..` `commondir`, linked worktrees, UNC/device/network targets, cross-volume targets, and any metadata root outside `PATH` (or MCP `_mcp_root`) as unsupported/unsafe before opening the target. Linux traversal uses `openat2` with `RESOLVE_BENEATH|RESOLVE_NO_SYMLINKS|RESOLVE_NO_MAGICLINKS|RESOLVE_NO_XDEV`; other Unix requires an equivalent handle-relative component walk with mount-ID enforcement before opening a child file; Windows uses `NtCreateFile` with the verified directory handle as `RootDirectory` and `FILE_OPEN_REPARSE_POINT` plus same-volume checks. An unavailable primitive fails `platform_no_follow_unavailable` rather than falling back to reconstructed paths. It reads only from already-opened regular-file handles. A same-user swap to a symlink, junction, bind mount, UNC path, alternate, or different inode is therefore unreachable or rejected before file content is opened.

Checksum-free controls and included content are semantically inert until an immutable-capture protocol succeeds: pre-handle metadata, two complete bounded reads/digests from the same handle, post-handle metadata, exact byte/digest equality, and unchanged identity/size/timestamps; both physical reads count against the appropriate metadata or source-input ledger. The staged worktree protocol is exact: (1) stable-double-capture `.git/info/exclude`; (2) at each traversed directory, set census-start immediately before enumerating its immediate bounded normalized names/types/identities, set path-observed on the first returned entry, and never open regular-file content yet; (3) identity-match the root `.git` entry to the already-authorized administrative handle and permanently exclude it from worktree descent/inclusion before ignore matching; (4) stable-double-capture that directory's `.gitignore`, set ignore-start immediately before root untracked-selection policy evaluation (including an empty candidate set), and parse only the immutable inherited/current stack; (5) never open ignored or administrative regular-file content, proven by separate canary hooks; (6) recurse only into allowed/potentially re-includable directories, stable-double-capture selected source files, set ignore-completed only after all selection and untracked-completed only after its stable captures, including a successful empty set; then (7) revalidate every visited census/retained handle before semantic use. Excluded directories are not enumerated below their immediate entry when reinclusion is impossible. Controls/refs/config/packed-refs use their same bounded channel; required `.git` metadata remains readable only there. Instability is incomplete; capture remains explicitly non-atomic across files. Checksummed indexes/objects/packs additionally use their integrity rules.

Local config has a closed, duplicate-aware policy after Git-style ASCII case-insensitive section/key canonicalization; singleton semantic keys differing only in case are duplicates. The exact repository-format matrix is: format 0 requires both hash extensions absent and means SHA-1; format 1 permits absent/`sha1` `extensions.objectformat` with absent compat and means SHA-1, `objectformat=sha256` with absent compat for ordinary SHA-256, or `objectformat=sha256` plus `extensions.compatobjectformat=sha1` for the transition format. Compat without SHA-256, any other value, and every missing/duplicate/cross-field-invalid transition declaration are refused. `core.bare` must be false; `core.worktree` is absent or resolves exactly to `PATH`; and boolean `core.filemode`, `core.symlinks`, `core.ignorecase`, and `core.precomposeunicode` are applied conservatively. `core.filemode=false` suppresses worktree executable-bit noise while index-versus-tree mode changes remain visible; `core.symlinks=false` treats an index-declared symlink's safely opened regular-file bytes as its logical link payload without following them; ignorecase/precompose modes require an exact spelling or one uniquely normalized match and reject collisions/ambiguous case or normalization changes rather than infer. The reader bounded-ignores without emitting values only `core.logallrefupdates`; `remote.<name>.{url,pushurl,fetch,push,mirror,tagopt}` except promisor/partial-clone keys; `branch.<name>.{remote,merge,rebase,description}`; and `user.{name,email}`, `commit.gpgsign`, and `tag.gpgsign`, because none changes this read-only storage interpretation. It refuses include/includeIf, sparse-checkout state, autocrlf/eol/attributes/excludes/fsmonitor/untracked-cache transforms, hooks/helpers/filters/credentials/SSH/proxy/url rewrites, alternates/replace/grafts, promisor/partial-clone, reftable/worktreeConfig/refStorage, command-valued keys, every other `extensions.*`, and every unlisted key. Values are never emitted. Vanilla `git init`, normal clone, Windows platform-key, mixed-case duplicate, each format-matrix arm, and each apply/ignore/refuse/duplicate arm have independent fixtures.

The frozen storage subset is repository formats 0/1; SHA-1/SHA-256 plus the exact transition combination above; the exact public ref grammar; index v2–v3; loose zlib objects; pack v2 with SHA-1/SHA-256 idx v2 and SHA-256 transition idx v3; commit/tree/blob/tag objects; and bounded OFS/REF delta resolution. Index v4 is explicitly refused because its prefix-compressed pathname framing is not implemented. The v2/v3 core-entry table accepts only stage-0 entries with recognized `100644|100755|120000|160000` modes, their fixed entry structure, strict UTF-8/NFC path of at most 32,768 bytes, NUL pathname terminator, name-length field equality (or the `0xFFF` long-name sentinel), and entry-start-relative 8-byte padding. V2 requires `CE_EXTENDED` unset. V3 parses the extra word only when `CE_EXTENDED` is set, then refuses `CE_SKIP_WORKTREE`, `CE_INTENT_TO_ADD`, a zero/noncanonical extra word, and every unknown/reserved extended bit. Both versions refuse stage 1/2/3, `CE_VALID`/assume-unchanged, sparse-directory mode, malformed padding/path length/UTF-8/normalization collisions, and every unknown/reserved base flag as `repository_format_unsupported`; a non-sparse-index sparse checkout is therefore refused even without `sdir`, never misreported as deletion. Index extensions `TREE`, `REUC`, `EOIE`, and `IEOT` are length/range-validated then ignored because they do not alter core entries; `link`, `sdir`, `UNTR`, and `FSMN` are refused; unknown uppercase optional extensions are length/range-validated and skipped, while unknown lowercase/mandatory extensions are refused. Reftable, shallow state, alternates/http-alternates, partial clones/promisors, replace/graft state, sparse-directory entries, index v4, unknown repository extensions, and missing objects fail `repository_format_unsupported` or `lazy_fetch_refused`, incomplete/exit 2. System/global config, PATH, helpers, hooks, filters, remotes, shells, credentials, and network are never consulted because no Git configuration or program is executed.

All parsing is from bounded byte readers. Config is 1 MiB; each control/ref is 4 KiB; at most 10,000 loose refs/4 MiB aggregate; packed-refs is 8 MiB; index is 16 MiB; at most 10,256 metadata files; logical metadata is 64 MiB and double-capture physical reads are 128 MiB; pack indexes retain their 128-file/32 MiB cap. The reader rejects from size metadata before cap+1 parsing/checksum work, while still reading cap+1 only where the file size is not trustworthy. Every index/pack-index checksum is verified. Before any object from a consumed pack is trusted, stream at most 256 MiB for that pack and 512 MiB aggregate through a 64 KiB buffer to verify the pack trailer hash and the index-recorded pack hash over the exact main SHA-1/SHA-256 domain; verify the consumed entry CRC over its packed bytes. Separately, actual loose-zlib/pack-entry compressed bytes consumed for semantic decoding have a cumulative 64 MiB physical cap, while decompressed object bodies, every repeated delta input/output, and diff/hunk intermediates have one cumulative 128 MiB logical cap; neither ledger refunds on release or cache eviction. SHA-256 transition-index main/compat tables and trailer domains follow the official transition spec. Every loose or packed semantic object is also accepted only after recomputing the repository-format OID over `type + " " + decimal_size + NUL + content` and constant-time matching the requested OID; pack offsets, delta bases, expanded sizes, and instructions are range/operation-checked before allocation/use. Exceeding any integrity/object ledger is incomplete, never complete-with-unverified or uncharged data. Pack data stays behind no-follow regular-file handles and is random-read/streamed, never mmaped or copied wholesale. One ledger caps all metadata/pack/object/worktree bytes, files, pack indexes, visited commits/trees/objects, delta depth, live buffers, tracked paths, diff/rename operations, and deadline before and after every fixed 64 KiB read/decompress chunk and each allocation/loop. Commit/tree traversal and tree-to-tree discovery are implemented by the same reader; rename detection starts from a no-renames add/delete set and runs only in the bounded in-process similarity matcher. The anchored safe-ignore walker follows the staged protocol above. Submodules are opaque and never recursed.

The impact cap is the shared metadata/pack/object ledger above plus 256 root commits, 20,000 total visited filesystem/history/object entries, delta depth 64, 5,000 tracked paths, 32,768 UTF-8 bytes per repository path, 64 ignore files/10,000 rules/2 MiB aggregate ignore bytes/10,000,000 non-backtracking ignore-matcher transitions, `max_changed_file_events=256` and `max_changed_symbol_events=1,024` counted across all phases, 32 MiB logical and 64 MiB physical double-captured source/worktree/ignore bytes, 64 MiB semantic-object physical reads/128 MiB logical expansion+diff bytes, 2 MiB/100,000 lines per accepted source/object, 64 MiB incremental live-buffer ceiling, 2,000,000 parser nodes, 10,000,000 bounded diff/rename operations, 32,768 bytes per caller string, 60-second default/300-second maximum, and 8 MiB final output. Unsafe nested links/mounts, metadata/content/path swaps, special files, invalid/unrelated refs, missing promised blobs, unsupported repository storage, unsupported grammar, ambiguous rename/delete, cap, deadline, or resolver gaps emit lower-bound incomplete output and never false-clear zero. Event-gated control/ref/index/loose-object/pack/index/worktree swaps include local, same-filesystem bind-mount, and UNC canaries and assert the canary file-open hook is never reached. Mid-read restored-size/mtime and hybrid-valid-ref/worktree/source/ignore fixtures must fail stable double-capture; cross-file mutation fixtures require the explicit non-atomic disclosure rather than a false atomic claim. Many-small-object and repeated-delta cap+1 fixtures independently trip physical and logical cumulative ledgers. CRC, pack trailer, index pack-hash, and transition-domain mismatches must fail independently. Both success and all failure arms create no child, scratch, or filesystem mutation.

Task 14 registers CLI/native/MCP `PATH` in the shared path-domain matrix; refs are opaque. Bridge failure occurs before Git/parser/filesystem/MCP dispatch, emits the full error envelope, and makes no changes. MCP re-confines the detected Git top-level under `_mcp_root()` and requires it to equal the requested canonical root; it refuses scope ascent, nested links/reparse points, and swap events.

Projection and impact each have a pre-merge consumer/value gate backed by hand-authored bidirectional gold fixtures. Projection must answer frozen dependency/coverage-gap tasks without source rescans and beat the pre-stated legacy map workflow on correct answers and tool-call count; twenty paired cold/cold and warm/warm AB/BA runs also require both median and p95 no worse than 1.5× the same-state legacy map control with process startup included symmetrically. Impact uses an outcome-independent frozen sample containing positive and leaf/negative historical patches, hand labels, and a candidate-independent control committed before candidate output. That control materializes manifest-verified H/B/C after-provider roots and executes existing path-reading symbol-impact from a hash- and source-digest-pinned Task-13 artifact, never from candidate/current production imports: H for commit, B for staged, and C for unstaged/untracked events. Candidate and control prove byte-identical path/mode/content-or-gitlink manifests and share head-kind scope, ignore bytes, attribute/global-config noncoverage, and stable-non-atomic disclosure. Accuracy and post-preparation tool calls are compared on those identical providers. Twenty paired cold/cold plus warm/warm AB/BA analysis-only latency repetitions exclude snapshot capture/materialization/verification from both arms and include analysis process startup in both or neither. Failure retires only that row before publication.

Task 14C exposes exactly the surviving actions through one always-on read-only `tg_graph` tool. Branch table: both survivors → actions `project|change`, MCP `1.11.0`, populations `60/14`; projection only → action `project`, same version/populations; impact only → action `change`, same version/populations; neither → no tool/version/population change from `1.10.0` and `59/13`, only non-releasing research/docs. The exact surviving branch is status-stamped and independently reviewed before implementation. No variant accepts arbitrary query language, mutation, execution, persistence, remote/multi-root graph, output path, workflow directive, or event replay.

For a survivor, the wheel pins MCP exactly to reviewed runtime 1.28.1. TG owns three validation layers before graph dispatch: a bounded duplicate-aware raw-stdio classifier before MCP/Pydantic request validation, an outer `CallToolRequest` handler before FastMCP's catch/stringify handler, and the same pure validator on direct calls. The raw tokenizer is an iterative single-pass scanner over the existing 64 MiB frame cap with maximum nesting 64, maximum 65,536 structural tokens, and maximum 1,024 decoded UTF-8 bytes for a response-eligible string ID. It strictly decodes JSON escapes and Unicode scalars incrementally before comparing every routing/correlation key, method value, and tool name to its fixed semantic value; raw and escaped spellings of the same semantic key therefore share one duplicate/ambiguity state. The executable matrix enumerates `method`/`meth\u006fd`, `tools/call`/`tools\/call`/`tools\u002fcall`, `params`/`par\u0061ms`, `name`/`na\u006de`, `tg_graph`/`tg_\u0067raph`, `arguments`/`argu\u006dents`, and `id`/`\u0069d` rather than relying on a generic escape claim. It stores no arbitrary token values, uses a fixed 64-entry context stack, and represents top-level ID state as `absent | sole-bounded-candidate | duplicate` rather than retaining occurrences; raw-plus-escaped same-value or conflicting ID keys both saturate to duplicate in either order. On depth/token excess it sets a classifier-error bit and continues a nonallocating saturating lexical/resynchronization scan through the already bounded frame so later top-level method/name/ID fields remain order-independent and nested or quoted raw/escaped `tg_graph`/`id` decoys cannot be promoted. After the full scan, depth/token/error disposition applies only to an exact or ambiguous semantic graph target; a definitively non-graph frame, including a capped legacy tool call with semantically escaped method/name, enters the original legacy path unchanged. An unexpected scanner exception that prevents definitive classification is contained at the no-log raw boundary and silently discards without response/model/forwarding. Recoverable Unicode/escape errors likewise do not stop structural target/ID resolution. A tokenizer-valid graph frame runs MCP 1.28.1's exact `JSONRPCMessage` validation, then mirrors session `ClientRequest` validation over the request's alias-preserving JSON dump, all inside separate no-log tries. Response eligibility requires exactly one semantic top-level ID key whose value is either a strict integer in `[-9223372036854775808, 9223372036854775807]` or a string within the 1,024-byte decoded limit that independently passes strict JSON-escape decoding and Unicode-scalar validation. Integer sign/digit-length/range validation precedes conversion and occurs inside the no-log eligibility gate; an exact 4,301-digit raw ID proves the converter is never called before lexical rejection. Absent, duplicate (same or conflicting, including raw/escaped aliases), malformed-escape/lone-surrogate/overlength string, overrange or unconvertible integer, null, bool, fractional, object, and array IDs take precedence over every other error and are silently discarded because no unambiguous safe response correlation exists. An eligible ID plus a graph-target depth/token cap violation returns the same fixed `-32602`/`invalid graph arguments`, absent data/result; an ineligible ID plus that violation silently discards. Other eligible failures use the same fixed response and preserve the decoded semantic string ID. A fully valid graph request must pass both models and reach the outer handler. Mapping arguments then face exact no-coercion/additional-properties/action/type validation. All definitively non-graph legacy frames and notifications retain existing handling byte-identically. Canary fixtures cover every enumerated raw/escaped semantic field before/after caps, field-local raw-plus-escaped duplicate aliases, sole escaped-ID positive/error cases, raw-plus-escaped same/conflicting ID duplicates in both orders and capped/uncapped, name-before/name-after list/string/number/boolean/null containers, wrong field values, duplicate/extra keys, lone-surrogate value/key/ID, malformed escapes including unique string IDs, positive ordinary escaped-string and valid surrogate-pair IDs, signed-64-bit integer boundaries/overflows plus the 4,301-digit pre-conversion arm, string-ID/depth/token cap−1/cap/cap+1 with target and ID on both sides, capped legacy calls with semantically escaped real targets and nested/quoted raw/escaped decoys, tokenizer-valid wrong-JSON-RPC-version and wrong-client-request failures, per-layer call counts, stdout/stderr/log silence, no forwarding/dispatch/read/mutation, and legacy startup/call availability.

Public documentation includes an interoperability cookbook showing how external graph runtimes may consume surviving read-only evidence alongside prepare, edit verification, evidence receipts, and cooperative claims. It does not claim tensor-grep schedules those nodes or authorizes returned hints. Shortest trace, cross-repository topology, workflow/causal execution, replay/fork, PDG, and graph mutation retain explicit owners, threat/value gates, and reopen triggers.

## 6. Error and security model

- Typed caller paths receive grammar/size preflight first; when a domain bridge is active they are translated, then canonicalized/confined in the target domain before use. Absent provenance retains direct confinement.
- Symlinks are refused at artifact destinations before resolution.
- Baselines and receipts are bounded before JSON parsing.
- Unknown schema major versions and unknown required policy fields fail closed.
- Digests use canonical JSON and bind the repository/revision/policy subject.
- No shell command strings are constructed; any future validation runner accepts an executable plus argv under an allowlist, fixed cwd, deadline, and output cap.
- MCP, backend, receipt verification, ledger locks, validation execution, native assets, installers, and migration changes require an independent adversarial `SHIP | FIX-FIRST(file:line, repro, minimal fix)` review.
- Local CPU-heavy evaluation and cold Cargo work are prohibited; decisive matrices run in GitHub Actions/cloud.

## 7. Delivery and review loop

For each implementation slice:

1. Refresh GitHub/main/PyPI truth and enforce the WIP/release gate.
2. Create or rebase an isolated worktree branch.
3. Write the smallest red contract test first.
4. Confirm the red failure is for the intended missing behavior.
5. Implement the minimum change.
6. Run focused tests with explicit timeouts.
7. Run scoped Ruff, preview-format, and mypy checks locally.
8. Run an independent specification review, then an independent quality/security review.
9. Fix every finding and repeat with a fresh reviewer until `APPROVE`/`SHIP`.
10. Open one PR, attach review evidence, wait for CI, then drain only after the latest main publish is complete.
11. Re-verify the merged artifact and the published wheel.
12. Update task board, backlog, handoff, contracts, and dogfood receipt table.

## 8. CEO-, financial-, and external-resource gates

The following are planned but not autonomously executed:

- #72 public benchmark claim;
- #131/#169 GPU hardware, asset-profile promotion, or public performance claims;
- #48 native-front-door startup architecture;
- #77/F9 automatic ledger enforcement in agents or CI;
- #255 native dedup/FFI, int8, warm-server, or GPU investment selection;
- paid cloud/GPU capacity or any action with a direct financial commitment.

The campaign may create evidence and decision records for these items. Execution requires a separate user decision because it changes spend, public claims, or established CEO-gated scope.

## 9. Completion definition

The campaign is complete only when:

- all AI-actionable items above are shipped or retired with evidence;
- `CODE-GRAPH-PROJECTION` and `CHANGE-IMPACT` are shipped with complete/partial honesty, deterministic projection, CLI/native/MCP equality, and published-artifact receipts, or are evidence-retired by the plan's falsification gates rather than represented as delivered;
- every independent thinktank and final code/security gate is clean;
- all PRs are merged through an open release gate;
- latest `main` is green;
- the latest required version is present on GitHub and PyPI;
- a clean-environment published-wheel table passes every wheel-visible changed contract, and separately labeled exact-merged-SHA CI/source receipts pass every internal-only changed contract;
- `docs/TASK_BOARD.md`, `docs/BACKLOG.md`, `docs/SESSION_HANDOFF.md`, `AGENTS.md`, and affected contract/docs files agree;
- CEO/financial items remain explicit decisions, not falsely reported as complete.

## 10. Thinktank audit record

This section is updated after each independent review round.

- Round 1 architecture: `FIX-FIRST` — aggregate dirty-status hashing could not identify same-path edits; MCP surface state was not frozen at import; the plan duplicated ledger claim identity; the Rust removal fork ignored public downstream consumers; Java was absent from cross-file resolution; and the multi-root public API was undecided. All six corrections were incorporated before the next review round.
- Round 1 security: `FIX-FIRST` — self-asserted identity could suppress overlaps; keyless digests were described too strongly; strict output allowed arbitrary external writes; MCP anchor confinement could widen; ignored-file coverage was unstated; manifest reads had path races; and the writer census missed `replace_with_retry`. All seven corrections were incorporated.
- Round 1 TDD/evaluation: `FIX-FIRST` — tracker CI could not know live GitHub state; #859 lacked the actual historical red arm; Rust faults were nondeterministic; native commands missed the native CI census; WARN was unpinned; boundaries/concurrency were ambiguous; workspace exit codes conflicted; language red arms could pass through regex; Task 11 was underspecified; workspace MCP registration was incomplete; and final dogfood rows were not exhaustive. All eleven corrections were incorporated.
- Round 2 architecture: `FIX-FIRST` — the dirty manifest did not bind the staged index or enumerate nested untracked files, and the workspace partial/error schema contradicted itself across service, CLI, and MCP. The plan now records content-addressed stage-0 index identity, fails closed on unmerged stages, uses `--untracked-files=all`, and defines one exact shared envelope.
- Round 2 security: `FIX-FIRST` — the reclaimable lease could not fence a resumed stale holder; Windows no-follow hashing was optional; receipt trust disclosures were outside the signed component; and the writer ratchet omitted direct destination sinks. The plan now requires a crash-released OS fence on every claims mutation, mandatory platform-safe opened-handle hashing or `INCOMPLETE`, signed component-local disclosures, and an enumerated sink census.
- Round 3 architecture: `FIX-FIRST` — Task 8 omitted the concrete fence/helper files and did not distinguish a stable fence inode from reclaimable lease metadata; Task 10A omitted Java's live registry seam in `repo_map.py`. The plan now names the claims-only stable fence artifact, every implementation/test file and cross-process proof, and Java's registry edit explicitly.
- Round 3 architecture re-review: `APPROVE` — the index manifest, shared workspace envelope, stable claims-only fence, and Java registry dependency were found coherent with no remaining architecture blocker.
- Round 3 security: `FIX-FIRST` — executable-bit and index-flag mutations could evade the Git oracle; duplicate JSON keys could create cross-parser trust ambiguity; standard-library sink aliases could evade the writer census; and MCP field injection made the 8 MiB boundary contradictory. The plan now binds worktree mode/index flags, rejects duplicate keys at all depths, resolves every enumerated sink alias against an independent inventory, and measures final transport bytes after injection.
- Round 3 TDD/evaluation: `FIX-FIRST` — edit schemas and reason precedence were not exact; later tests could hide behind import failures; Git fixtures/copy semantics and baseline caps were ambiguous; Windows parent junctions could evade leaf tests; the fence primitive/timing/RMW API lacked an executable contract; signed-disclosure tests could prove only cryptography; the writer red arm was circular; and cross-transport equality/dogfood was incomplete. Exact schemas, behaviorless-shell sequencing, real-Git/platform tests, one baseline cap, callback RMW fencing, semantic signed/keyless tests, mutation controls, and individual final-artifact rows are now specified.
- Round 4 security re-review: `APPROVE` — all prior Git-oracle, duplicate-key, writer-alias, MCP-cap, reparse, fence, malformed-input, and trust-disclosure blockers were closed under the documented cooperative-agent threat model.
- Round 5 architecture: `FIX-FIRST` — exact Git IDs excluded SHA-256 repositories; unconditional callback publication broke legacy no-op release; and overlap/ticket reason mappings conflicted. The schema now carries object format with 40/64-hex consistency, claims callbacks return explicit `WRITE`/`NO_WRITE`, and every reason maps exhaustively to verification verdict, ticket status, and exit.
- Round 4 TDD/evaluation: `FIX-FIRST` — untracked index flags and prepare/receipt-component schemas remained inexact; global pytest `-x` could mask adapter/language reds; zero-root CLI wording contradicted its JSON contract; and source-only writer tests were misattributed to wheel dogfood. These now have exact nullability/types, node-ID red sequencing, parser-optional/service-required roots, and separately labeled merged-source CI receipts.
- Round 5 TDD/evaluation re-review: `APPROVE` — exact schemas/object formats, behavior-specific red arms, real-Git and mandatory Windows fixtures, callback-fence semantics, semantic receipt tests, transport equality, and evidence attribution were clean.
- Round 5 security final re-review: `APPROVE` — no remaining security regression; final proof correctly separates wheel-visible behavior from exact-merged-SHA internal contracts.
- Round 6 architecture: `FIX-FIRST` — public edit command argv/scope/descriptor inputs were not frozen, prepare-to-snapshot policy was unspecified, and the exact evidence component had no production ingress. The plan now fixes Python/native argv, accepts only owned exact baselines, normalizes scope and caller-supplied structured validation deterministically, defines every prepare projection field, and adds bounded `tg evidence emit --edit-verification` coverage through both front doors.
- Round 6 security: `FIX-FIRST` — verification results could be replayed across repositories/revisions, file-backed trust inputs did not all share the safe opened-handle reader, and result-size accounting excluded the final transport. The plan now binds repository/revision/dirty identity, routes every trust input through one confined reader, and caps exact final-wire bytes.
- Round 6 TDD/evaluation: `FIX-FIRST` — compiled-native help/argv nodes were outside the native CI census, evidence behavior REDs could be masked by an unknown option, validation descriptors lacked executable boundary rules, and `partial_other` projection was ambiguous. The plan now puts every native node in `test_native_*.py`, sequences behaviorless shells before one RED per behavior, freezes descriptor identity/type/path/collision bounds, and pins mixed-source projection semantics.
- Round 7 architecture: `FIX-FIRST` — verifier identity inputs and strict baseline publication were not fully frozen. The plan now requires an external baseline SHA-256 anchor, structured target/blast comparison, exact validation inputs, and handle-relative create-if-absent publication with no path fallback.
- Round 7 security: `FIX-FIRST` — handle-relative no-clobber proof and evidence-subject provenance were incomplete. The plan now mandates Unix `openat`/`linkat` and Windows root-handle-relative publication, single-capture receipt subjects, producer-version preservation, and binding of the exact verification-result digest.
- Round 8 architecture: `FIX-FIRST` — the file-only verify-to-evidence handoff changed the dirty digest and made a normal round trip reject itself. The plan now freezes bounded `--edit-verification -` stdin ingestion and proves signed/keyless round trips with separately checked producer and consumer exits.
- Round 9 security: `FIX-FIRST` — a 0→0-only round trip could claim to prove pipe-status safety without exercising a failing producer. The plan now pins 0→0, 1→0, valid 2→0, and invalid-input consumer-2 arms and explicitly disallows unchecked shell-pipeline status masking.
- Round 10 post-approval live deep-dive: `FIX-FIRST` — Task 2's global “shipping entry” parser had no canonical grammar, #90 was incorrectly treated as wholly shipped, required contract/comment/#859-receipt files were omitted, the CEO set was not frozen, and a version-token-only handoff refresh could preserve obsolete content. Program 0 and Task 2 now define one canonical live-status index, exact source decisions and file scope, mixed #90 handling, unique CEO/demand ownership, and substantive handoff reconciliation before implementation.
- Round 11 backend/writer deep-dive: `FIX-FIRST` — the writer population had three live violations plus generated-source/scope-resolution blind spots; Rust hardening overstated direct-file failures and placed private fault seams in an external test; and Python `CPUBackend` retained two unsafe `TypeError` compatibility retries already retired in its `RustCoreBackend` twin. Programs 1 and Tasks 3/5 now pin the exact populations, genuine production REDs, private Rust seam placement, unchanged public semantics, and one-call Python adapter contract.
- Round 12 amendment thinktank: `FIX-FIRST` — the tracker lacked a parseable version and complete demand population, #89/#859 transitions were ambiguous, the writer plan remained vulnerable to parent swaps and no-clobber races, generated helpers/sanctions were not closed-world, the Rust public type was not mechanically pinned, and fixed-string fallback could still mask native `TypeError`. The design and Tasks 2/3/5/15 now freeze those contracts and require independent per-node RED/green receipts before the next review.
- Round 13 amendment thinktank: `FIX-FIRST` — the closed-world tracker omitted F5–F8 and three program identities, #859 had no executable post-merge status path, Task 3 omitted its shared-helper files/tests, pre-resolution and scaffold mkdir remained race bypasses, the CPU AST census could pass first-run green, and DD-004 conditionally authorized unplanned code. Program IDs/lifecycles, anchored directory creation, exact file/test scope, independent census RED, and demand-only DD-004 are now explicit.
- Round 14 amendment thinktank: `FIX-FIRST` — #77 was worded as the sole CEO row instead of the sole F9 alias owner, an actionable #89 reproduction had no build owner, F10 conditionally authorized unplanned removal, and closure PRs were not themselves required to pass/merge/reverify while preserving implementation-PR attribution. Those lifecycle and status contradictions are now removed.
- Round 15 convergence: architecture, adversarial security, and TDD/evaluation independently returned `SHIP` on the same execution-contract bytes. This status/audit stamp is the only subsequent change and is subject to one final exact-hash review before Task 2 dispatch.
- Round 16 exact-hash re-review: `FIX-FIRST` — architecture found a cyclic prepare-service dependency, impossible pre-registration verify/evidence subprocess tests, and an under-specified workspace schema; security found a parent-swap/split-fence path, unbounded/out-of-workspace project-config reads, missing implementation-PR tracker transitions, and an unowned public Rust leaf-symlink decision. The design and implementation plan now move prepare extraction before Task 6 consumers, move public subprocess proof to Task 7, type the complete workspace envelope, anchor claims RMW to one verified directory handle, bound/confine every Task 11 config reader, make tracker files/lifecycle mandatory for Tasks 3–13, and add canonical `RUST-REPLACE-SYMLINK`. Re-review is required on the new exact hashes.
- Round 17 amendment re-review: `FIX-FIRST` — the first claims-fence rewrite accidentally omitted the concrete `flock`/`LockFileEx` acquisition/hold/release contract and fresh-repository anchored state-directory creation; TDD also found the moved native/evidence round trips were still scheduled after their producer mappings. The plan now freezes handle-relative first-use creation, exact platform lock primitives/order/lifetime/revalidation, fresh-zero-state swap controls, and Python/native/signed/keyless REDs before each producer mapping. Re-review is required on the new exact hashes.
- Round 18 TDD re-review: `FIX-FIRST` — the first post-registration behavior test could only fail on the sentinel while the plan simultaneously forbade sentinel REDs, and Step 3 still said to add a native suite already created in Step 1. Task 7 now labels the sentinel arm as wiring-only, requires per-state verdict/exit/digest and native-forwarding mutation REDs across Python/native/signed/keyless surfaces after each mapping, restores green before continuing, and reruns rather than recreates the native suite.
- Round 18 convergence: architecture, adversarial security, and TDD/evaluation returned `SHIP` on the same amended execution-contract bytes. This status/convergence stamp is the sole subsequent plan change and must itself receive one final exact-hash confirmation before Task 2 resumes.
- Round 19 live Task 2 amendment trigger: `FIX-FIRST` — bounded treatment/control probes invalidated the approved assumption that #89/#90's WSL arm was a non-defect. Raw WSL paths passed to the selected Windows-native executable produced #89 `path_not_found` and #90 a false-clear zero; translating only the typed path with `wslpath -w` made the same binary find the expected search results and six AST matches. Code deep-dives also found unfixed `run` and MCP rewrite twins. Round 18 approval is therefore expired. Program 0A and Tasks 2A–2C now require explicit launcher provenance, grammar-typed translation, bounded no-shell bridging, fail-closed/no-mutation behavior, twin coverage, ordered tracker lifecycles, and published WSL dogfood. Three-seat amended-plan review and final exact-hash confirmation are pending.
- Round 19 first amended-plan council: three seats returned `FIX-FIRST` on design hash `7FADA2F644595145BA27C1A6CE840EE63BC8A273B4F756C389BEDCFDD3C9B31D` and implementation hash `71122A86B28FB0DFEF20BD990A10D21EABE3947BFE068785F3111A01D6DA1481`. Must-fixes were a nonexistent MCP file path/incomplete direct-meta field census, split Python/Rust ownership and budget reset, caller-domain authorization against Windows roots, non-versioned/inconsistent output evidence, reserved `WSLENV` collisions, substitutable/under-bounded `wsl.exe` process handling, mutation-capable index misclassification, unsafe key/audit alias/read handling, invalid mutation-test wording, omitted Task 2 merge gate and Task 2C Windows census, later Tasks 6–8/12–13 path fields outside the registry, MCP version collisions, and wheel-only attribution for an installer change. The design and plan now define one owner matrix/vector corpus, exact evidence/reasons, trusted Job-contained bridging, target-domain authorization/safe effects, full field/version lifecycle, unchanged-test mutations, Task 2 merge precondition, non-skipped Windows CI, and tagged-installer/release-asset dogfood. Re-review is required on new exact hashes.
- Round 20 amended-plan council: three seats returned `FIX-FIRST` on design hash `7712E56BB868A2F4CFF8DCFB10BA74F4BE4A52610F9CCFADF10D1B0D10CF7775` and implementation hash `D11CAB8B586A9C4400DBFD85A330979F0E3D9B549B4578E35DBFF1F22318A3D1`. Remaining gaps were search pattern/ignore-file operands, Python/Typer run ownership, the complete existing evidence-emit path set/environment fallbacks, exact launcher distro/final-WSLENV failure contracts, Task 2B anchored baseline/suppression publication, path-domain mappings inside edit/workspace closed schemas, repo-relative edit scopes, stale pre-translation workspace confinement prose, and later tasks omitting shared registry files. All are now explicit with field spelling/cap fixtures, full evidence no-effect and signed/keyless proofs, target-domain writers, schema/reason/nullability/precedence/cap mappings, WSL-aware confinement order, and additive shared-file scopes. Re-review is required on new exact hashes.
- Round 21 amended-plan council reviewed design hash `FA1230205A92903BBE110AF1D00D946FD0B8F6447C4A8580515DEB196E63119E` and implementation hash `45CB180D6525C793C1D17D2DB38381BAE635BAE07D2CD747B56DF5F011C1417B`. Adversarial security returned `SHIP`; architecture and TDD returned `FIX-FIRST`. Remaining gaps were an unnamed/unscoped anchored scan writer, accidental rejection of existing regular-file overwrite semantics, exactly-empty `WSLENV` ambiguity, semantic REDs for currently unreachable attached/bundled option spellings, and an impossible executable embedded-NUL environment fixture. Task 2B now names and owns `_index_lock.atomic_write_bytes_anchored` plus its test module before Task 3 consumes them, characterizes and preserves each scan writer's regular-file replace behavior while refusing links/directories, treats exactly-empty `WSLENV` as zero entries while rejecting empty tokens only in nonempty values, stages registration RED/green before semantic RED for unreachable spellings, and confines NUL rejection to pure byte-parser vectors. Re-review is required on new exact hashes.
- Round 22 amended-plan council reviewed design hash `0C46C3D05317C86F4A6DCD8443EA5356D7C2C921D98E01F25F7C3DD19EC8CB86` and implementation hash `973D65EAF97D0C77D079F58F11D2CB1675220800577E023828558C549D6F9535`. Adversarial security returned `SHIP`; architecture and TDD returned `FIX-FIRST`. The remaining gaps were unreachable Python/Typer batch/audit/key tests and bridge, anchored-writer, and Task 2C security behaviors described after their implementation. Ownership now matches the live CLI: Typer owns only its positional root while direct-native owns root/batch/audit/key. Every bridge, anchoring, alias/race, and no-effect semantic node is now required RED before its production behavior; an absent seam permits only one import/registration RED plus a behaviorless shell, and post-green mutations remain supplemental sensitivity proof. Re-review is required on new exact hashes.
- Round 23 amended-plan council reviewed design hash `5FA65C9AB617C35464599CB3FF7EAB7704B0A71175CFDB67E90C7CB94B04C854` and implementation hash `C3474932B37EA5A475598ACEC8B61D04FEAB81850D41E11E279D5E513EA10CAA`. Adversarial security and TDD returned `SHIP`; architecture returned `FIX-FIRST` because Task 3 still put public writer semantics and race tests after caller routing. Task 3 now extends Task 2B's already-shipped helper seam before routing, runs every affected writer's public overwrite/no-clobber/link/failure/order/Event-swap node independently RED, then routes one writer at a time and restores green. Re-review is required on new exact hashes.
- Round 24 amended-plan council reviewed design hash `8BA84CE45E7AC4104C94A475F94A65DF2266A76F06CDDD8EE65C9CB242BE71E4` and implementation hash `03C94573C64219DC0407911B28DF3969CA84A30175D63B672764A4698EB2B81A`. Adversarial security and TDD returned `SHIP`; architecture returned `FIX-FIRST` because an optional Task 3 leaf-symlink-entry replacement contradicted Task 2B's shipped shared-helper refusal contract. The alternative is removed: every artifact writer, including regular-file overwrite routes, refuses live, dangling, and reparse leaf links with unchanged trees. Re-review is required on new exact hashes.
- Round 25 amended-plan council reviewed design hash `AEAD82D77BEE5A403A1F593D5BA1A60AE56267D900C3B221966D9D18D0E2E47D` and implementation hash `5642642BFB3AA2D50D29ADF15AB87B2EEC142C7B7E2EB7ED7B14935714721539`. Architecture, adversarial security, and TDD/evaluation all returned `SHIP`. Before the status-only convergence stamp, the user added a current graph-coding research/build request, so that approval remains valid evidence for the prior scope but is not approval of the enlarged campaign.
- Round 26 graph-scope research amendment: Exa primary-source research separated code/dependency graphs, workflow/control graphs, and causal run graphs. Three independent seats then audited tensor-grep's live architecture, threat boundaries, and the smallest falsifiable TDD slice. Their common recommendation is Program 5: a bounded `CodeGraphProjectionV1`, a Git-diff-driven `ChangeImpactV1`, and one read-only fixed-schema MCP tool; workflow execution, graph databases, replay/fork, cross-repository graphs, PDGs, and mutation remain explicitly deferred. The amended exact bytes require a fresh three-seat `SHIP` round and final status-hash confirmation.
- Round 27 graph-scope council reviewed design hash `97A03A715312A90F3E238EAA8B972FFEA6CD6105522D5437499CE28C0A5529CD` and implementation hash `A1BBD57DE8A206A5FF539ED83382F737C8FAC2BA70F4B0A17AF728FDE6C51BAD`; all three seats returned `FIX-FIRST`. Architecture found unconditional MCP publication after a value retirement, ambiguous validation-command production, incomplete node/endpoint/ID/source/rename/truncation/trust contracts. Security found executable/network-capable Git configuration, Git worktree reads bypassing no-follow capture, MCP scope ascent and nested-link escape, post-materialization caps, ignored-input/authorization overclaim, duplicate/truncation contradictions, undefined repository identity, error leakage, and a read-only write escape hatch. TDD found masked semantic REDs, underspecified IDs/trust, self-referential graders, outcome-selected value samples, biased latency comparison, no projection value gate, and post-publication retirement. Program 5 and Task 14 now freeze repository-scoped canonical IDs, exact node/edge/source/trust/omission tables, opaque untrusted validation hints, closure-preserving caps, hardened no-network Git metadata/object access with anchored worktree diffing, exact scope/zero-mutation/privacy behavior, independent bidirectional gold oracles, outcome-independent paired value/latency gates, pre-merge retirement, and a survivor-dependent MCP branch table. Re-review is required on new exact hashes.
- Round 28 was interrupted before any verdict because the orchestrator's official-Git-doc self-audit found that ordinary commands still read local config, so merely overriding dangerous keys did not prevent include-path access. Those review hashes are intentionally invalidated. The plan now adds a current Git security floor, trusted metadata ownership/DACL checks, bounded no-follow local-config allowlisting with all includes/helpers/promisor state refused, fixed plumbing only, and identity/digest revalidation around every child. A new exact-hash round is required.
- Round 29 graph-scope council reviewed design hash `CAEDFC137640B6BD29179BC6BD06ECE3DE3B054E9CF4DF5728824F89442F9704` and implementation hash `EC99E4039BEF96E75CE750631EEAD17CFFF11E423A30C430743C6CAF6A3484AB`; all three seats returned `FIX-FIRST`. Architecture found an impossible failed-candidate tracker lifecycle, cross-candidate foundation dependence, a dynamically delegated Git floor, repository IDs that diverged on orphan refs, falsely exact early-cap omissions, a repository-node final-wire contradiction, and ambiguous invalid MCP actions. Security found local-config and alternates TOCTOU after preflight, missing Git-object content-address verification, and invisible executable/type mode changes. TDD found the hardened foundation scheduled after its first consumer, no projection latency gate, and an unconditional final `SHIPPED` transition for retired candidates. Task 14 now lands tracker/oracle truth first; gives exactly the two graph candidates a tested `READY`→`RETIRED` closed-PR exception; makes each candidate carry a pre-consumer production foundation; freezes Git 2.55.0 and `git_unavailable`; uses a private immutable scratch Git context with handle-anchored object views; recomputes requested object IDs; captures modes/types; rejects orphan root sets; represents omission exactness honestly; retains the repository node state-consistently; defines MCP InvalidParams precedence; adds paired projection latency; and separates shipped from retired final closure. Re-review is required on new exact hashes.
- Round 30 graph-scope council reviewed design hash `4068FA51AA187D4EA6EEC44155D51257BFF5AA353B072108D99D42AD106EC6FD` and implementation hash `6F1EEDE209EA79DDD8453B27095DE47B98339F514BF4B72E629EFA8182C794A4`. TDD/value returned `SHIP`; architecture and adversarial security returned `FIX-FIRST`. Architecture found an unreachable 20,000-edge cap under the contains/defines-only node invariant, a repository-node rule accidentally applied to node-less impact output, complete impact trust preserved after evidence-clearing output truncation, and an ID preimage whose qualified name was not exposed normatively. Security proved the same-user-mutable scratch/config/index, child loose-object/pack reads, cleanup residue, and unobservable Git-internal traversal still violated the threat/resource model. Program 5 now removes Git subprocesses and scratch entirely in favor of a handle-relative, in-process, checksum/OID-verifying, byte/object/depth/memory/operation-bounded Git-format reader; freezes its supported/refused repository-storage table; requires canary handles never open; lowers the edge cap to a reachable 4,000 with a valid fixture; scopes the repository-node rule to projection; downgrades impact trust on output rebuild; and binds node `name` to the canonical qualified name. Re-review is required on new exact hashes.
- Round 31 graph-scope council reviewed design hash `A153C3AF5617B9A7398259BA8B4DD3968B7E408706DD362E630F72251A94D309` and implementation hash `D707591B27BBE925262900386264C406978B30A25F98B738CDDBAF3253D46588`; all three seats returned `FIX-FIRST`. They found inherited map deadline/file-cap flags could bypass fixed graph limits and MCP null deadline was ambiguous; control/ref/index metadata lacked exact byte/file caps; the post-`rev-parse` REF grammar and several Git index extensions were undecided; Unix `openat(O_NOFOLLOW)` did not stop bind mounts/`..`; checksum-free controls lacked a stable-capture protocol; and consumed pack CRC/trailer/index-hash verification was incomplete. Program 5 now has a parameter-source-aware strict graph-v1 argv branch, finite 60-default/300-maximum CLI/MCP deadlines, fixed file caps, exact metadata and streamed pack-integrity ledgers, a closed HEAD/full-OID/fully-qualified-ref grammar, an exhaustive index-extension table, main-worktree-only metadata confinement, Linux `openat2` beneath/no-link/no-mount plus equivalent platform gates, double-read/digest stable capture for checksum-free controls, and per-entry CRC plus end-to-end consumed-pack hashes. Re-review is required on new exact hashes.
- Round 32 graph-scope council reviewed design hash `A28084FFBC038DCDAEB782466133094E7AD0491D7FBAA3DC6F0A391879F746DA` and implementation hash `3F04E19D0C65B69B9640B7A3CF17C81F22AB7697D5DF83289476D0B0085C7F5C`; all three seats returned `FIX-FIRST`. Architecture found no executable apply/ignore/refuse policy for ordinary local Git config, making vanilla repositories either rejected or semantically misread. Security found undecided stage/assume-valid/skip-worktree/intent-to-add index entries and an ignore matcher whose nominal file/rule caps still allowed unbounded CPU inside one adversarial match. TDD found the detailed fixtures scheduled after wholesale foundation implementation rather than independently RED before each behavior. Program 5 now freezes a duplicate-aware config-key table with conservative platform semantics, a stage-0-only index-entry/flag table, a 10,000,000-transition non-backtracking ignore matcher with in-loop deadline checks, parser-versus-post-dispatch deadline behavior, and nine ordered one-semantic-at-a-time foundation microcycles whose boundary/integrity fixtures must fail for their intended reason before production. Re-review is required on new exact hashes.
- Round 33 graph-scope council reviewed design hash `C1EC6D148ABEDC922139FB002D03F8EB7CC4E1B51E7232817DB6BA0BA6EB997D` and implementation hash `13C5A60D68B550251B1306BFE1359944912934FFF9890B08466845826F0C973D`; all three seats returned `FIX-FIRST`. Architecture and TDD found the config table refused `extensions.compatObjectFormat=sha1` before the promised SHA-256 transition-index-v3 path, masking its detailed REDs; architecture also proved that a local-only raw reader could not honestly claim Git-porcelain visibility in the presence of attributes or global excludes. Security found checksum-free worktree/source/ignore reads could form a false-clear hybrid snapshot and that accepted index v4 lacked its prefix-compressed pathname parser contract. Program 5 now freezes the repository/object/compat cross-field matrix, independently reaches transition main/compat corruption arms, narrows impact to explicitly disclosed repository-local raw/local-ignore-only coverage, double-captures every checksum-free source and ignore input under separate physical-byte caps, and supports only index v2/v3 while testing v4 refusal. Re-review is required on new exact hashes.
- Round 34 graph-scope council reviewed design hash `CD374EE86C7E6F2CDFD41A8C7BD64DE4CCEBF7AB8ED1D54DD1E253936E756C80` and implementation hash `1FB01D3FAE6FE5C15F3C12FBEEB26D67AA61B94A4ABC6AD69B0940CD3A3C3714`. TDD returned `SHIP`; architecture and adversarial security returned `FIX-FIRST`. They found capture-first contradicted ignore privacy/selection, per-file stability overclaimed cross-file atomicity, explicit commit heads shared an untracked-worktree coverage label, the design's Git-diff control disagreed with the raw implementation control, and many small compressed objects or repeated deltas had no cumulative semantic expansion cap. Program 5 now freezes a census→hierarchical stable-ignore→selection→stable-content→revalidation protocol with unopened ignored-content canaries; discloses stable-but-non-atomic capture; makes commit/worktree coverage and booleans exact; aligns both value controls; and adds 64 MiB physical plus 128 MiB logical no-refund semantic-object/delta/diff ledgers with decisive cap+1 fixtures. Re-review is required on new exact hashes.
- Round 35 graph-scope council reviewed design hash `CFA6E2297B4B10662E21407E1A4B203A713E6431F4516E28D27B1609B45A5BBB` and implementation hash `75E5CEA69831489B9583F7A1C5B057A15FA60C5F425040A95205354A10792A44`. TDD returned `SHIP`; architecture and adversarial security returned `FIX-FIRST`. They found projection's exact coverage omitted its non-atomic disclosure, requested worktree mode falsely set execution booleans on pre-walk errors (with MCP null ambiguity), and explicit-head enrichment could call current-worktree map/resolver/test/policy services after an object-only diff. Program 5 now puts fixed capture-consistency fields directly in projection coverage; freezes absent/null/string/malformed head truth plus actual pre-/mid-/post-walk phase evidence; and requires immutable object-backed parser/resolver/test/hint providers for explicit heads with byte-invariance and unopened-worktree canaries. Re-review is required on new exact hashes.
- Round 36 graph-scope council reviewed design hash `8CDFFD5B77C8D6D0E58B2ACD3FFE2BB3962A616992E5A4B8091AEA2A1624353E` and implementation hash `18054C06A7B9C5399D74AFB67F7D4EBA257EDC483B35050983FA498F5449DB1A`. TDD returned `SHIP`; architecture and adversarial security returned `FIX-FIRST`. They found typed MCP schemas made malformed-type graph envelopes unreachable, the source walker did not hard-exclude the administrative directory, projection coverage omitted the selected path universe and executed phases, census-start flipped after rather than before its attempt, and explicit-head enrichment did not freeze base/head relation ownership. Program 5 assigned schema/type/action-enum violations to a single strict pre-service boundary; hard-excludes the authorized administrative identity; freezes tracked-plus-nonignored-untracked projection scope with attempt/observation/completion phases; and uses both base and head for change attribution while retaining only head-provider relations and hints. Re-review is required on new exact hashes.
- Round 37 graph-scope council reviewed design hash `3755D7BB74335CE8B3A4B1C111327DC79CDE3DE07D49783AD91760B33C9CC656` and implementation hash `3570D8A4D3D68A30E72F03C30F04FA172FDF0F11DE0EA323083CC5D0944306D5`. TDD/value returned `SHIP`; architecture and adversarial security returned `FIX-FIRST`. Architecture proved omitted-head base→worktree semantics could false-clear an A→B staged change followed by B→A worktree restoration and could not represent divergent A→B→C state. Security reproduced that MCP 1.28.1 FastMCP registers `call_tool(validate_input=False)`, accepts extra fields, and can echo malformed canaries through a `ToolError` result instead of protocol InvalidParams. Program 5 now emits separate identity-bearing `commit|staged|unstaged|untracked` transition events over immutable base/index/worktree providers, with phase-aware affected evidence and decisive A/B/A plus A/B/C fixtures; it also replaces framework validation reliance with a TG-owned low-level CallToolRequest/direct-call validator, exact advertised schema, fixed no-data `-32602`, and raw-stdio no-disclosure controls. Re-review is required on new exact hashes.
- Round 38 graph-scope council reviewed design hash `315250A5ACDCD9D8DD878C0247AFA980F242402255355FAEC4895A5E08D25F9F` and implementation hash `7F4C37E617B79E7DF71E9298138B8A36183D757CFDF1FF4C5128DB7E08216AB3`. All three seats returned `FIX-FIRST`. TDD proved the existing symbol-impact control would rescan live A/C rather than the candidate's staged B snapshot, biasing correctness and latency. Security found the private MCP 1.28.1 handler contract was not protected by the shipped `mcp>=1.27.2,<2` range or clean unlocked-wheel testing. Architecture found cross-phase exact/heuristic confidence merge ambiguous and the 256 changed-file cap ambiguous between paths and phase-event records, with mixed-source cutoff behavior undefined. Task 14 now pre-materializes candidate-independent manifest-verified after-provider control roots and excludes preparation symmetrically from timed arms; Task 14C exactly pins MCP 1.28.1 and tests unlocked wheel metadata/runtime; affected confidence uses a conservative lattice; event caps/omissions use explicit event units; and truncation filters sources, drops empty records, and forces lower-bound trust. Re-review is required on new exact hashes.
- Round 39 graph-scope council reviewed design hash `3D639E5CDDFD2E92AED96B032FE0EE882B2C903174672BBA1997CA11A9E0B8F5` and implementation hash `52B1F4CA8227E00FE69631E1DE4745FE62CCD8119FE1261E98E60D246B5D80D2`. All three seats returned `FIX-FIRST`. TDD found the MCP exact-pin production edit preceded its governance/wheel-metadata RED. Architecture found two stale unique-file/symbol cap phrases and proved the control could drift by importing a candidate-modified live symbol-impact helper. Security demonstrated a non-object raw stdio `arguments` canary leaks during MCP `ClientRequest.model_validate`, before the low-level `CallToolRequest` wrapper. Task 14 now records the exact-pin metadata RED before production, uses event-unit cap language everywhere, executes the control from a hash- and file-digest-pinned Task-13 artifact with no candidate production import, and classifies bounded raw stdio graph frames before MCP/Pydantic validation with fixed no-data errors and no logging. Re-review is required on new exact hashes.
- Round 40 graph-scope council reviewed design hash `06CA9E18856F77DAE5D7596957A997E4A8F1A022384547CC5FCE9DAE04ECB0A9` and implementation hash `6FFF0F47EDEEEBA4C24F6F8BF4FE7E174BEEACCDCF574DA9BB58EA441B648259`. Architecture and TDD/value returned `SHIP`; adversarial security returned `FIX-FIRST`. Security proved a lone-surrogate graph value passed Python's classifier, then failed MCP's Pydantic model and leaked the canary from its warning path. Task 14C now keeps exact downstream MCP model validation inside TG's no-log raw boundary for every identifiable/ambiguous graph target and pins lone-surrogate value/key plus malformed-escape canaries. Re-review is required on new exact hashes.
- Round 41 graph-scope council reviewed design hash `1CA458FC481F2C992FF9E0C3B97F7F491A3A7FD226E1D9AFACF4FAF44D7433B0` and implementation hash `866ED37F8C4821495E47A066C84FBB6C2661971D695E08E5BC8134CEB84A1BE5`. Architecture and TDD/value returned `FIX-FIRST`; the security seat failed before an anchored verdict and grants no approval. Architecture found recoverable malformed content before `name:"tg_graph"` could leave target state unset and re-enter the leaking legacy path. TDD found tokenizer rejection and exact-model catching could mask each other's mutation tests. The raw boundary now continues recoverable-error tokenization through target resolution regardless of field order, tests name both before/after arguments, asserts zero model calls for tokenizer failures, and gives the exact-model catch an independent tokenizer-valid/wrong-JSON-RPC-version RED. Re-review is required on new exact hashes.
- Round 42 graph-scope council reviewed design hash `562A7E4E06E1F1A8F5878DC5DEF6FCCF5CA4B2F56DDEAB51C915554A43A03C31` and implementation hash `74372B644961A1201E4562532BA34EBB64B55DDF1687D1D8A46B773C88522408`. Architecture and TDD/value returned `SHIP`; adversarial security returned `FIX-FIRST`. Security found that JSON-RPC parsing alone could classify an ID-less graph-shaped frame as a notification, which then failed later `ClientNotification` validation and leaked through framework logging. The raw boundary now also runs exact `ClientRequest` validation before forwarding, emits the fixed error only for a valid request ID, silently drops invalid/no-ID graph frames, and pins ordinary legacy notification behavior. Re-review is required on new exact hashes.
- Round 43 graph-scope council reviewed design hash `7685C439D951C404E2E8E0E56AC256876E6D94467520DE324BC5247D50D8269D` and implementation hash `974C7DEEB6E0AA8BF02E3A79AFB8E89C4ED96EF9A70DEED32F254EC14EB3B02E`. Architecture returned `SHIP`; adversarial security and TDD/value returned `FIX-FIRST`. Security found direct validation of a `JSONRPCRequest` model instance did not mirror MCP session validation and would reject every valid graph call. TDD found “string-container” contradicted valid string request IDs and error precedence for invalid/no IDs was unstated. The raw boundary now validates `ClientRequest` over the request's alias-preserving JSON-mode model dump, pins a fully valid pass-through control, accepts only string/strict-integer IDs, and makes invalid/no-ID silent discard precede tokenizer/model error handling. Re-review is required on new exact hashes.
- Round 44 graph-scope council reviewed design hash `3DF558746BE9A292A59BA14AA5A53D123FF4A96DCDFFAE60506D5A3F417B3588` and implementation hash `D73197545C4E5304B9474A87A5AAD36E75924A17693B1D306167ECD27CCC4348`. Adversarial security and TDD/value returned `SHIP`; architecture returned `FIX-FIRST`. Architecture found response eligibility did not define duplicate top-level IDs despite duplicate-aware tokenization. The raw boundary now requires exactly one valid top-level ID and silently discards same-value or conflicting duplicates before tokenizer/model error handling, with crossed zero-output/log/model/forward tests. Re-review is required on new exact hashes.
- Round 45 graph-scope council reviewed design hash `29C1D9D688E7E529FB1BF0243FF3D4E522F6AF85FF9C7229F985FB0085402937` and implementation hash `B9427A4DB44AF7F3901FDEA3BA5738581EEC16D9305BA5F8A60814635563B412`. TDD/value returned `SHIP`; architecture and adversarial security returned `FIX-FIRST`. Both found that a unique string ID containing a lone surrogate or malformed escape remained response-eligible and could leak, fail serialization, or kill the stdio task at the direct error writer. Response eligibility now independently requires strict JSON-escape decoding plus Unicode-scalar validity for a sole string ID; malformed string IDs silently discard before every writer/model/error path, with crossed zero-output/log/model/forward tests. Re-review is required on new exact hashes.
- Round 46 graph-scope council reviewed design hash `9BF3A88A8B89D6306AB7818F6A54BA4D58FA6675CE1EBD737EE1C3BC6C1136FC` and implementation hash `0B19A6EA67A148A5F7712D87328009DEA501C1F133D5D0D0376CF2B31F49A958`. TDD/value returned `SHIP`; architecture and adversarial security returned `FIX-FIRST`. Architecture found the negative escape tests could be satisfied by rejecting every escaped string ID, including valid ordinary escapes and surrogate pairs. Security found an arbitrary-length numeric ID could fail Python integer materialization before the sanitized writer. The contract now includes positive decoded semantic-ID controls and valid surrogate pairs, an explicit signed-64-bit integer range validated before conversion, boundary/overflow crossings, and mutations that reject all escapes/surrogate code units. Re-review is required on new exact hashes.
- Round 47 graph-scope council reviewed design hash `7A4B56F19C975C175B08F902BED609641FE1EAC870626F16514B9525F1DBB683` and implementation hash `B6D19EDAE2D08882A4D199A3A736EC21EC7276C999F938F9C4979F63AD19D091`. TDD/value returned `SHIP`; architecture returned `FIX-FIRST`; the adversarial-security seat failed before a verdict and grants no approval. Architecture found that boundary-overflow cases did not prove lexical length rejection occurs before integer conversion: a 4,301-digit ID could still raise before silent discard. The plan now pins that exact raw case, zero converter/model/writer/log/forwarding calls, and an early-conversion mutation. Re-review is required on new exact hashes.
- Round 48 graph-scope council reviewed design hash `93B04530CACB3A287A60DE4B54DAC4B15014459ECA7EB7210F5273A584BD0B68` and implementation hash `5A483C6ED91D18E4F6CA53FABFC1702A5E808C43AF478BE822A814DD133DCE84`. Architecture and TDD/value returned `SHIP`; adversarial security returned `FIX-FIRST`. Security confirmed the numeric-ID repair but found “bounded tokenizer” did not define nesting/token/state-memory caps or exception containment, so a recursive or occurrence-retaining implementation could exhaust the reader before the no-log catches. The raw classifier is now explicitly iterative and fixed-memory with depth 64, 65,536 structural-token, and 1,024-byte decoded-ID caps; duplicate state saturates, cap overflow continues bounded resynchronization for order-independent ID/target resolution, and unexpected scanner failures silently discard. Cap-edge/order/eligible/ineligible fixtures and recursion/allocation mutations are required. Re-review is required on new exact hashes.
- Round 49 graph-scope council reviewed design hash `4010C96FC88500EED088D052AC8D128ECDFC35ED7550FC0CB3E90FFF8B1EAF54` and implementation hash `BFF258730F62A23B55F728B5541A4A0A4E46C45706C5F08B386F717A90BC78D5`. Adversarial security and TDD/value returned `SHIP`; architecture returned `FIX-FIRST`. Architecture found the resource-cap rule globally discarded a valid capped legacy call, contradicting byte-identical legacy behavior. The classifier now completes resynchronization, applies cap/error disposition only to exact or ambiguous graph targets, and forwards definitively non-graph frames through the unchanged legacy path. Depth/token-cap legacy controls place the real target before/after the capped value and include nested/quoted graph/ID decoys plus global-discard/decoy-promotion mutations. Re-review is required on new exact hashes.
- Round 50 graph-scope council reviewed design hash `CF73E5541EDED48E0FA3EEB37430EBE2A46924E19DC640E5535214E41777B4C5` and implementation hash `6A2A44233C4717F5A6203B5D5F4C4BCEDF414F7A26C43CB367EB191E29B4742F`. Architecture and TDD/value returned `SHIP`; adversarial security returned `FIX-FIRST`. Security found a raw-spelling classifier could misclassify semantically escaped routing fields such as `par\u0061ms`, `na\u006de`, or `tg_\u0067raph` as legacy and forward malformed graph arguments to the framework. Routing comparisons now use strictly decoded semantic strings, raw/escaped aliases share duplicate state, and capped/uncapped escaped-target, duplicate-alias, and escaped nested-decoy fixtures plus a raw-comparison mutation are required. Re-review is required on new exact hashes.
- Round 51 graph-scope council reviewed design hash `6C022672CA11270681CC96C08AD37D63DD27ED2DD7A59AF40B3A0EF26232CE7E` and implementation hash `257B1505A262F2583B611A5155E0A6ABE49A7388EFA1018FE0379035BF16DEC3`. Adversarial security returned `SHIP`; architecture and TDD/value returned `FIX-FIRST`. They found the executable matrix named escaped params/name/tool values but did not pin escaped method keys/values, arguments, or the top-level ID key, so a partial semantic decoder could still enter legacy handling or fail to saturate duplicate IDs. The matrix now explicitly enumerates every routing/correlation field, raw-plus-escaped aliases and ID duplicates in both orders, capped/uncapped placement, escaped legacy controls, and field-local raw-comparison/alias-collapse mutations. Re-review is required on new exact hashes.
- Round 52 convergence: architecture, adversarial security, and TDD/value independently returned `SHIP` on design hash `2BCF36632960F23410B32475264B442DED85E23FB4797DEF705CD8F350716F98` and implementation hash `7A3892B400E13E1E641A9CC9CB9E2C79D3D3767D8E5E043287B6D24000E35D8B`. This status/convergence stamp is the sole subsequent plan change and must itself receive one final exact-hash confirmation from all three seats before Task 2 resumes.
- Round 53 exact-hash confirmation: architecture, adversarial security, and TDD/value returned `CONFIRMED-SHIP` on status-stamped design hash `CE117FE092BDBE5402E3A6A62251F75DF5E863E94B0DA656B733DD17876CC4E0` and unchanged implementation hash `7A3892B400E13E1E641A9CC9CB9E2C79D3D3767D8E5E043287B6D24000E35D8B`.
- Round 54 pre-build Task 2A deep dive: `FIX-FIRST` — the approved file list omitted the direct Rust Win32 dependency/lock files and actual native pattern/ignore/output sites; `-f/--file` gates were deliberate correctness quarantines rather than mere registration gaps; existing `Popen`/Rust process helpers could not retain the suspended primary-thread handle; `WinVerifyTrust` alone did not prove a Microsoft signer; the installed wrapper's own `/mnt` assumption could fail before translation; NDJSON and exact Windows-CI node attribution were underspecified. Program 0A and Task 2A now add the exact dependency/source/test/CI scope, real pattern-file/ignore semantics before gate removal, low-level process/signer identity, sibling-shim/configurable-automount resolution, terminal-only provenance, and a frozen node manifest. Three-seat re-review and a new status-stamp confirmation are required before Task 2 merges or production begins.
- Round 55 Task 2A amendment council reviewed design hash `9542454CD02C67EB2956AEAB10B5AB839EB2D2113DA3B07895E0AB667F19980D` and implementation hash `22C418C2F17A703EE72EC9969B940F0B157031A942DF61449BCE9AB205099577`. TDD/value returned `SHIP`; architecture and adversarial security returned `FIX-FIRST`. They found contradictory translate-before-read language, an impossible zero-child oracle for post-launch failures, foreign-sibling execution risk, trust not bound cache-only to the held identity, unnamed pattern/ignore limits, and a manifest without a frozen artifact/job proof. Program 0A and Task 2A now freeze one exact five-stage domain sequence, child-class failure taxonomy, managed-copy-only sibling launch, held-handle/cache-only Microsoft predicate, numeric streaming caps plus a committed rg-derived semantic table, and exact manifest/receipt files with job ownership. Re-review is required on new exact hashes.
- Round 56 Task 2A amendment council reviewed design hash `8D430440A08EBE6E6DAF76A99BDAE234945B8F276E4C5290CB582EA5A9B00255` and implementation hash `2ECDD9F2A93A1E5A32E917AA562A85D4B50CE74D74B63657D0801A9028EC51B1`. TDD/value returned `SHIP`; architecture and adversarial security returned `FIX-FIRST`. They found that stock `wsl.exe` can be catalog-signed rather than embedded-signed; post-`CreateProcessW` Job/resume/pipe setup failures were missing from cleanup taxonomy; an old tensor-grep-owned shim could survive beside a newly foreign executable; stable libtest does not natively supply the promised closed-world JUnit receipt; and ignore files had byte caps but no aggregate rule-count bound. Program 0A and Task 2A now freeze held-handle embedded/catalog verification, handle-return-defined cleanup with boundary injection tests, owned-shim fail-closed retirement/PATH removal, dedicated pytest JUnit plus a versioned stable-Rust exact-node runner, and 16 KiB/65,536 ignore-rule bounds. Re-review is required on new exact hashes.
- Round 57 Task 2A amendment plan iteration status update: repaired implementation language now uses the explicit zero-vs-post-handle cleanup taxonomy and PATH-route bookkeeping wording; status-stamped design hash `E417E4D54F4E23BE751E549256FB68D1B3F548504F1CFB8CEA33ADD05E898A78` and implementation hash `97563FC149773C870E330C7861D078911EC94D428A78532A5CCD937DEB4A5FDD`.
- Round 58 Task 2A amendment plan iteration status update: tightened manifest binding to committed run/job/runner/command-digest witness rows; status-stamped design hash `F7D4C9B1894674A3381B352A2E0BB39E00731A040FD8BDA5F73F4ADE184AEB95` and implementation hash `1F1A270D1E8626161B7CCC92E5A56248BB18B35064A0372FC8986267FC4DEE44`.
- Round 59 canonical reconciliation: the newer Round-58 worktree already contained the held-handle signer, process-cleanup, pattern/ignore, and exact-node-manifest repairs, so the older root copy was not promoted. The canonical pair now adds only the surviving current-byte gaps: bounded `InstallerShimReceiptV1` authority with retained handles and token-preserving PATH conflict refusal; retained System32 parent identity; `SearchInputLedger` at 64 MiB/10,000,000 transitions/300 seconds from request entry; and current-run-bound `NativeCiReceiptV1`. Fresh exact-hash review and final status-stamp confirmation are pending before production or merge.
- Round 60 adversarial closeout: architecture returned `SHIP` on the Round-59 transaction/receipt shape, but security returned `FIX-FIRST` on eight enforceability gaps and TDD returned `FIX-FIRST` on one mixed-counter boundary gap. This revision names the protected CNG-backed receipt root, TxR registry primitive, PATH directory-identity comparison, exact offline WinTrust/Microsoft-root policy, non-breakaway Job limits, every ledger front door, combined inclusive counters, and Actions-environment/JUnit/Rust-census re-derivation. Cursor Auto then found and closed three stale operative contradictions, TDD returned exact-byte `SHIP`, Sol returned substantive `SHIP`, and Sol confirmed the status-stamped pair. PR #911 push/CI and all product code remain separate later artifacts.
