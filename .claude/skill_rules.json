{
    "version": "1.0",
    "description": "Project-local skill triggers for tensor-grep (consumed by the global ~/.claude/hooks/skill_activation_gate.py engine; merged over global rules, invisible outside this project). Seeded 2026-07-17 from the project's local skill library (12 initial entries; 15 as of 2026-07-23) -- see AGENTS.md's skill index for the current, authoritative skill count and set (this file's own entry count has no governance test, so treat it as a snapshot, not a promise); expand from gate_fires.jsonl conversion data. This file is harness config for the activation hook, not a product contract, and is invisible to tests/unit/test_skill_index_sync.py.",
    "skills": {
        "tensor-grep-debugging-playbook": {
            "priority": "high",
            "intentPatterns": ["\\b(fix|debug|investigate|diagnose|why is)\\b.{0,60}\\b(bug|error|failure|crash|panic|test)\\b"]
        },
        "tensor-grep-release-and-positioning": {
            "priority": "high",
            "intentPatterns": ["\\b(ship|cut|push|publish)\\b.{0,30}\\b(release|v\\d|crates|pypi|npm)\\b"]
        },
        "tensor-grep-build-and-env": {
            "priority": "medium",
            "minKeywordHits": 1,
            "keywords": ["cargo build", "maturin", "build fails", "rust toolchain"]
        },
        "tensor-grep-gpu": {
            "priority": "medium",
            "minKeywordHits": 1,
            "keywords": ["cuda", "gpu index", "gpu search"]
        },
        "tensor-grep-semantic-search-campaign": {
            "priority": "medium",
            "minKeywordHits": 1,
            "keywords": ["semantic search", "embedding index", "lsh"]
        },
        "tensor-grep-validation-and-qa": {
            "priority": "medium",
            "keywords": ["validation", "qa pass", "regression", "benchmark suite"]
        },
        "tensor-grep-workspace-dogfood": {
            "priority": "medium",
            "minKeywordHits": 1,
            "keywords": ["dogfood", "tg callers", "tg blast", "tg scan"]
        },
        "tensor-grep-ledger": {
            "priority": "medium",
            "minKeywordHits": 1,
            "keywords": ["tg ledger", "claim symbols", "coordinate agents", "advisory lock", "overlapping edits"]
        },
        "tensor-grep-prepare": {
            "priority": "medium",
            "minKeywordHits": 1,
            "keywords": ["tg prepare", "edit readiness", "prepare an edit", "one-call edit"]
        },
        "tensor-grep-find-and-route": {
            "priority": "medium",
            "minKeywordHits": 1,
            "keywords": ["tg find", "route-test", "hybrid search", "vocabulary mismatch"]
        },
        "tensor-grep-multi-project-search": {
            "priority": "medium",
            "minKeywordHits": 1,
            "keywords": ["multi-project search", "workspace root", "unscoped search", "sibling repos"]
        },
        "tensor-grep-enterprise-review-bundle": {
            "priority": "medium",
            "minKeywordHits": 1,
            "keywords": ["review-bundle", "review bundle", "change review package", "audit-history"]
        },
        "tensor-grep-add-language": {
            "priority": "medium",
            "minKeywordHits": 1,
            "keywords": ["add a language", "new grammar", "tree-sitter grammar", "lang_registry", "register_language", "symbol-graph language", "onboard a language"]
        },
        "profile-guided-byte-identical-optimization": {
            "priority": "medium",
            "minKeywordHits": 1,
            "keywords": ["byte-identical", "cprofile", "profiling probe", "optimize hot path", "microbench", "warm dogfood", "cold path", "differential fuzz", "shipped wheel"]
        },
        "tensor-grep-large-repo-scale-campaign": {
            "priority": "medium",
            "minKeywordHits": 1,
            "keywords": ["deadline", "hang", "large repo", "timeout", "scandir", "unscoped search", "workspace scale"]
        }
    }
}
