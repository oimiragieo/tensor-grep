name: Benchmarks
on:
  pull_request:
    paths: ['src/**', 'tests/e2e/**']
jobs:
  benchmark:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      - run: pip install -e ".[dev]"
      - run: pytest tests/e2e/test_throughput.py -v --tb=short
